# Ruflo (claude-flow) + Agentic QE v3 — Setup & Repair Kit

A six-script kit that gets [`ruflo`](https://www.npmjs.com/package/ruflo) (the renamed `claude-flow`) and the standalone [`agentic-qe`](https://www.npmjs.com/package/agentic-qe) plugin into a known-good state inside [Claude Code](https://github.com/anthropics/claude-code), and keeps them there session to session.

If you have ever seen one of these symptoms, this kit is for you:

- `agentdb_controllers` reports **15+ controllers disabled** even though ruflo says it started cleanly
- `memory_search` returns nothing on a project that should have entries
- The Claude Code status line is **stuck on a stub** ("▊ RuFlo V3.x" with no live data) after running `aqe init`
- A fresh `npx ruflo@latest …` call **resolves a stale agentdb@1.x** from somewhere on disk
- After a Node.js / Claude Code / ruflo upgrade, MCP starts but the rich features (ReasoningBank, intelligence layer, swarm coordination) silently degrade

The scripts are **idempotent**, **dry-run-safe**, and **report what they change**. They are aimed at one box (the developer machine running Claude Code), not at production.

> **New to this kit?** Start with the section ["RuFlo vs Agentic QE — what does what"](#ruflo-vs-agentic-qe--what-does-what) below for a quick mental model of which tool owns which surface.

---

## Scripts at a glance

Every script is now invoked through the single `bin/ruflo-kit` dispatcher, which takes a `<command>` and a positional `<target>` codebase path (defaults to `$(pwd)`). The implementations live in `lib/` (renamed from the old flat `scripts/` layout).

| Command (`bin/ruflo-kit …`) | Implementation | Purpose | When to run |
|---|---|---|---|
| **`init <target>`** | `lib/init.sh` | One-shot, idempotent project initialization: prereq check → `ruflo init` → `ruflo memory init` → `agentic-qe init` → **`.claude` template backfill (Step 4.5)** → `fix-ruflo` (AgentDB patches) → `fix-statusbar` → `fix-aqe` → activation table → seed memory.db → 10-point verification. | First-time bootstrap of a new project, or after `--force` to wipe and re-init. |
| **`fix-ruflo <target>`** | `lib/fix-ruflo.sh` | 11-step diagnosis + repair of the ruflo MCP setup: patches `controller-registry.js`, removes shadow `agentdb`, fixes `.mcp.json`, re-registers MCP, cleans ghost state, updates `agentic-qe`. | First-time setup, after any Node/ruflo upgrade, whenever ReasoningBank or memory look broken. |
| **`fix-statusbar <target>`** | `lib/fix-statusbar.sh` | Restores the rich Claude Code status line that shows live ruflo + Agentic QE v3 stats. Survives `aqe init` clobbering. | Status line shows a stub, missing emoji, or no live numbers. |
| **`session <target>`** | `lib/session-init.sh` | Runs `fix-ruflo` + `fix-statusbar` in quiet mode, then verifies MCP, persistent storage, AgentDB controllers, native RuVector binaries. | At the start of every Claude Code session for full effective use. |
| **`upgrade <target>`** | `lib/upgrade.sh` | One-shot Phase-A upgrade: stops daemon, bumps global ruflo, wipes `~/.npm/_npx`, re-hydrates, re-runs `fix-ruflo`, then re-runs the activation layer via `init <target> --reactivate`. | When migrating across a major ruflo bump (e.g. 3.6 → 3.10). |
| **`health <target>`** | `lib/health.sh` | Growth-delta monitor. Captures the same metrics ruflo's statusbar shows (memory entries, SONA patterns/trajectories, MoE accuracy, ReasoningBank patterns, DB rows/sizes, daemon status, hive workers); compares against `.claude-flow/data/health-last.json` snapshot from the previous run; prints color-coded deltas; flatlines >24h warn; regressions exit non-zero (CI-friendly). | After any session where you want to confirm the loop is actually learning; in cron/CI to gate on growth. |

`init` and `session` orchestrate the others; the fix scripts are standalone. The init command handles **bootstrap** (calling `ruflo init` / `aqe init` for the first time); the session command handles **per-session maintenance** (assumes the project is already initialized). The dispatcher resolves `KIT_DIR` (this clone) vs `TARGET_DIR` (the codebase path you pass) via the shared `lib/common.sh`.

---

## RuFlo vs Agentic QE — what does what

The kit installs three separate npm packages that cooperate but own different surfaces. Confusion about which package owns what is the single most common source of "I thought this was broken" issues — the statusbar in particular splits its rows by domain (Tier 5), so reading the right chip for the right question matters.

**The three packages:**

| Package | Role | Owns | Database |
|---|---|---|---|
| `ruflo` (npm: `ruflo`) | MCP orchestration platform — agents, swarms, hive-mind, hooks, neural training, daemon | `.claude-flow/` config + state, MCP server registration, hooks runtime, swarm-state, hive-mind, neural patterns | `.swarm/memory.db` (sql.js + HNSW) |
| `agentic-qe` (npm: `agentic-qe`) | Quality engineering plugin — 60 specialist agents, 13 bounded contexts, SONA learning, MoE routing, ReasoningBank | `.agentic-qe/` config + state, ~115 `.claude/skills/`, 60 `.claude/agents/`, ~20 `aqe-*` commands, the "Agentic QE v3" section in `CLAUDE.md` | `.agentic-qe/memory.db` (sqlite + embeddings) |
| `agentdb` (npm: `agentdb`, pinned to `@3.0.0-alpha.10` — pre-pivot; do NOT bump to alpha.12+, see Patch 17.1) | Direct AgentDB primitives — attention, reflexion, skills, causal, learning-session tools | Project-scope MCP server entry (Tier 6.1); shares `.swarm/memory.db` with ruflo via @claude-flow/memory's `ReasoningBank` controller | (no separate DB — uses `.swarm/memory.db`) |

**MCP server layout in `.mcp.json`:**

```json
{
  "mcpServers": {
    "claude-flow":  { "command": "ruflo", "args": ["mcp", "start"], "autoStart": true },  // global bin, NOT `npx -y ruflo@latest` — see Patch 18
    "agentic-qe":   { "command": "aqe-mcp", "args": [] },  // live config uses the global `aqe-mcp` bin (NOT npx) — see Patch 22.3
    "agentdb":      { "command": "npx", "args": ["-y", "agentdb@3.0.0-alpha.10",  "mcp", "start"] }
  }
}
```

After `bin/ruflo-kit fix-ruflo <target>`, expect three servers (one auto-start, two opt-in via `.claude/settings.json`'s `enabledMcpjsonServers`). MCP chip rendering rule (Patch 9): chip total = deduped union of user-scope + project-scope servers; enabled = user-scope (always on) + (project-scope ∩ enabledMcpjsonServers).

**Statusbar row → package mapping:**

| Statusbar row | Package | Counter source |
|---|---|---|
| `🤖 Swarm [N/15]` | ruflo | `swarm-state.json` agents (ruflo's hive-mind workers) |
| `👥 Sub N` | Claude Code (NOT ruflo, NOT AQE) | `~/.claude/projects/<key>/<session>/subagents/*.jsonl` — counts your `Agent({ run_in_background: true })` calls |
| `🪝 N/M` | ruflo | `M = total hooks defined by ruflo` (currently 21 in the latest ruflo template — increases as ruflo adds new hook types). `N = hooks actively wired in `.claude/settings.json`'s `hooks` block`. When all defined hooks are wired, this renders `21/21` green. The "active vs defined" semantic was previously ambiguous; **clarification (Tier 6.2)**: this is `wired/defined`, not `pre-task active/scheduled`. |
| `💾 NMB` (Swarm row) | kit-total | All three DBs + hnsw.index — cross-domain summary |
| `🧠 N%` | ruflo | Real MoE **routing accuracy** from `ruflo hooks intelligence stats` (Patch 20.7 `RUFLO-INTEL-V1`). Was a `.swarm/memory.db` file-size proxy; pre-3.10.x BRAIN-INPUT-TOTAL-V1 no longer applies |
| `📶 SONA` | ruflo | SONA learning ladder: trajectories-driven 5-tier `[●●●○○]` + patterns + quality, from `hooks intelligence stats` / `neural/stats.json` (Patch 20.7) |
| `📊 AgentDB row` | **ruflo only** (Tier 5) | `.swarm/memory.db` vectors + size — no longer mixed with AQE state |
| `🧩 Plugins row` | ruflo (plugin scan) | `node_modules/@claude-flow/plugin-*` packages |
| `▊ Agentic QE v3 row` | **AQE only** (Tier 5) | `.agentic-qe/memory.db` patterns + trajectories + vectors + size |

**Rule of thumb:** if a chip changes on the AgentDB row, ruflo's swarm/memory layer changed. If a chip changes on the AQE footer, an AQE agent did something (captured an experience, scored a pattern, advanced a trajectory). Cross-domain summary chips (`💾 MB`, `🧠 %`) live on the universal `🤖 Swarm` row.

**What lives in each `.claude/` subdirectory:**

| Path | Owner | Purpose |
|---|---|---|
| `.claude/skills/` (~115 dirs) | mostly AQE | Test design patterns, AQE methodologies (qe-test-generation, qe-coverage-analysis, etc.) |
| `.claude/skills/agentic-quality-engineering/` | AQE | Half-init marker — must exist after `aqe init --auto` (Local Patch 6) |
| `.claude/agents/` (~60 dirs) | mostly AQE | Specialist agents (qe-coverage-specialist, qe-security-scanner, qe-test-architect, etc.) |
| `.claude/commands/aqe-*.md` (~20 files) | AQE | Slash commands the AQE plugin contributes |
| `.claude/helpers/statusline.cjs` | ruflo (patched by `fix-statusbar.sh`) | The statusbar renderer. On **aqe-3.10.x** the native render + the `AQE310-REALIGN-V1` data overlay (Patch 20.6) + `AQE-FOOTER-V2` drive the chips; the older per-chip sentinels (`VECTORS-HNSW-PATCH-*`, `MCP-COUNT-PATCH-V1`, `MEMORYMB-DBSIZE-V1`, `LEARNING-PATTERNS-V1`, `SUBAGENTS-SCAN-V2`, `BRAIN-INPUT-TOTAL-V1`) apply only to the **pre-3.10.x** statusline structure — fix-statusbar still attempts them but their anchors no-op on the new base (harmless). |
| `.claude/helpers/statusline-v3.cjs` | `fix-statusbar.sh` | Dual fallback when statusline.cjs crashes |
| `.claude/settings.json` | shared | Hooks block (ruflo), `enabledMcpjsonServers` allowlist (Claude Code), statusLine command |
| `.claude-flow/` | ruflo | Runtime: config, swarm-state, hive-mind, neural patterns, hooks intelligence |
| `.swarm/` | ruflo | memory.db + hnsw.index |
| `.agentic-qe/` | AQE | memory.db (qe_patterns, qe_trajectories, captured_experiences, embeddings, etc.) |

**Which script touches what** (read this when a script logs something surprising):

| Script | Touches ruflo state? | Touches AQE state? | Touches Claude Code? | Touches npx cache? |
|---|---|---|---|---|
| `init-ruflo-aqe-agentdb.sh` | yes (init + activation) | yes (init) | no | no |
| `fix-ruflo.sh` | yes (patches + MCP + state cleanup) | no (advisory only on legacy plugin) | yes (registers MCP) | yes (removes shadow agentdb<3) |
| `fix-statusbar.sh` | reads | reads | yes (statusline.cjs + settings.json statusLine) | no |
| `ruflo-session-init.sh` | yes (daemon auto-start) | no | no | no |
| `upgrade-2026-05-26.sh` | yes (daemon stop, ruflo upgrade, fix-ruflo, re-activate) | no (re-activate runs init but skips aqe via `--reactivate`) | no | **yes (wipes entire cache)** |
| `ruflo-health.sh` | reads | reads | no | reads (NAPI probe only) |

If you only remember one thing: `fix-ruflo.sh` is for the MCP and AgentDB/ReasoningBank substrate; `fix-statusbar.sh` is for what you can SEE; `ruflo-health.sh` answers "is the loop actually growing"; `init-...` is the one-shot bootstrap; `ruflo-session-init.sh` is the daily entry; `upgrade-...` is when ruflo itself bumps a major version.

---

## Prerequisites

- macOS (Darwin) or Linux. Tested on macOS arm64.
- Node.js ≥ 18 (recommended ≥ 22).
- `npm` reachable on the public registry, OR a local mirror.
- Claude Code CLI (`claude`) installed and on `PATH`. Optional — without it the MCP-registration step is skipped.
- Bash (these are `#!/usr/bin/env bash`, not POSIX `sh`).

The scripts touch only **user-scope** locations: `~/.npm/_npx`, `~/node_modules`, the project's `.mcp.json`, `.claude/`, `.claude-flow/`, `node_modules/`. They do **not** require `sudo`.

---

## Quick start

Clone the kit once, then point it at the codebase you want to set up. The kit (this clone) is `KIT_DIR`; the codebase path you pass is `TARGET_DIR` (defaults to `$(pwd)`):

```bash
# one time per machine — clone the kit
git clone <repo> ruflo-aqe-kit && cd ruflo-aqe-kit

# one time per project (bootstrap) — pass the target codebase path
bin/ruflo-kit init /path/to/codebase

# every Claude Code session afterwards
bin/ruflo-kit session /path/to/codebase
```

The bootstrap step is idempotent: it skips `ruflo init` / `aqe init` if it detects they have already been run, and only re-applies the patches. Pass `--force` to wipe state and re-init from scratch, or `--dry-run` to preview without changes.

That's the whole happy path.

If something looks wrong (controllers disabled, memory empty, status line dead), run:

```bash
bin/ruflo-kit fix-ruflo /path/to/codebase       # patches
bin/ruflo-kit fix-statusbar /path/to/codebase   # status line
# then restart Claude Code
```

A safe preview without changes:

```bash
bin/ruflo-kit init /path/to/codebase --dry-run
bin/ruflo-kit fix-ruflo /path/to/codebase --dry-run
bin/ruflo-kit upgrade /path/to/codebase --dry-run
```

---

## `init-ruflo-aqe-agentdb.sh` — first-time project bootstrap

A single entry point that brings a fresh project from zero to a fully working ruflo + AQE + AgentDB setup with the **self-learning + self-improving loop activated**. Each of its steps (1, 2, 3, 4, **4.5**, 5, 6, 6.5, 7, 7.5, 8, 9) is gated by an on-disk or `ruflo`-stat check so the script can be re-run safely.

| Step | Section | What it does | Skip condition |
|---|---|---|---|
| 1/9 | Prerequisites | Verifies `node`, `npm`, `ruflo` (or falls back to `npx -y ruflo@latest`), `aqe` (or falls back to `npx -y agentic-qe@latest`), and `claude`. Aborts if `node` or `npm` are missing; warns and recommends `npm install -g agentic-qe` if `aqe` isn't on `PATH`. | Never skipped. |
| 2/9 | `ruflo init` | Creates `.claude/` (skills, commands, agents, helpers, settings.json with 7 hook types), `.claude-flow/` runtime, `.mcp.json` registering the `claude-flow` MCP server. | Skipped if `.claude-flow/config.yaml` + `.mcp.json` + `.claude/skills/` already exist. |
| 3/9 | `ruflo memory init` | Creates `.swarm/memory.db` (sql.js / WASM SQLite) with vector embeddings (`Xenova/all-MiniLM-L6-v2`, 384-dim), HNSW indexing, pattern learning, temporal decay, and the AgentDB controller registry. | Skipped if `.swarm/memory.db` already exists. |
| 4/9 | `agentic-qe init` | Runs `aqe init --auto` (the canonical first-time flag per the [agentic-qe README](https://github.com/proffesor-for-testing/agentic-qe#quick-start)). This both **initializes the SDK** (`.agentic-qe/memory.db`, PersistentSONAEngine, DreamScheduler, Queen Coordinator, 14 domains) **and copies the project-level integration**: ~115 skills, AQE agents, AQE commands, plus an "Agentic QE v3" section appended to `CLAUDE.md` with policies for Integrity, Test Execution, and Data Protection. Prefers the globally-installed `aqe` binary; falls back to `npx -y agentic-qe@latest` if missing. Uses `--upgrade` when `--force` is passed. | Skip-check requires **both** `.agentic-qe/memory.db` **and** the AQE-specific marker `.claude/skills/agentic-quality-engineering`. If the DB exists but the marker doesn't, the script identifies a **half-init** (SDK ran but `.claude/` templates were never copied — happens with `--memory hybrid` or any non-`--auto` invocation) and re-runs `aqe init --auto --upgrade` automatically to repair it. |
| 4.5/9 | **.claude template backfill** (NEW, Patch 26) | Non-destructively backfills every agent/command/skill present in the global ruflo CLI template or the agentic-qe template but MISSING from `.claude/` (only ADDS, never overwrites). Fixes the AQE-first partial init (79/89 ruflo agents → full 23 categories). | Skipped under `--reactivate` (without `--force`). Adds nothing once `.claude/` is complete. |
| 5/9 | AgentDB patches | Shells out to `lib/fix-ruflo.sh` — applies the three `controller-registry.js` patches (ESM `path`, relative `agentdb` import, embedder argument) so ReasoningBank initializes correctly. | Never skipped (the patches detect already-applied state internally). |
| 6/9 | Statusbar restore | Shells out to `lib/fix-statusbar.sh` — restores the rich statusline that `aqe init` clobbered in step 4. | Never skipped. |
| 7/9 | **Activation table** (NEW) | Runs the 7 sub-steps that take the project from "files on disk" to "self-learning loop on". Each sub-step is independently idempotent and respects `--dry-run` / `--force`. See the dedicated subsection below for details. | Each sub-step has its own skip rule. The roll-up reports `<N> run, <M> skipped` out of 7. |
| 8/9 | Seed memory | Stores a single `init` entry in `.swarm/memory.db` so subsequent `memory_search` calls aren't empty. Pre-checks via `ruflo memory retrieve -k init`; if the key already exists, skips immediately (the SQLite-backed memory enforces `UNIQUE(namespace, key)`, so a blind re-store would always fail). If the key is missing, attempts to store with exponential-backoff retry (1s → 2s → 4s → 8s, 5 attempts) and a race-check between retries that treats a concurrent insert as success. | Skipped under `--dry-run`; also skipped (counted under "Steps skipped") on every re-run after the first successful seed. |
| 9/9 | Verification | 10-point check: `.mcp.json`, `.claude/settings.json`, `.claude/skills/`, `.claude-flow/config.yaml`, `.swarm/memory.db`, `.agentic-qe/memory.db`, **`.claude/skills/agentic-quality-engineering` (AQE half-init guard)**, `.claude/helpers/statusline.cjs`, **`.claude/agents` (ruflo full set backfilled — Patch 26 guard)**, and `ruflo mcp status` reachability. | Never skipped. |

#### Step 7/9 — Activation table sub-steps

The activation table is what turns a freshly-`init`-ed project (all-zero statusline) into a project where the self-learning + self-improving loop is actually running. It is the difference between "ruflo is installed" and "ruflo is alive".

| Sub-step | Command | What it produces | Skip condition |
|---|---|---|---|
| 7A | `ruflo swarm init --topology hierarchical-mesh --max-agents 15` | `.claude-flow/swarm/swarm-state.json` (coordinator + queue) | Skipped if `swarm-state.json` exists and mtime < 30 min |
| 7B | `ruflo embeddings init` | ONNX embeddings (`Xenova/all-MiniLM-L6-v2`, 384-dim) downloaded and registered | Skipped if `ruflo embeddings status` reports a `Provider:` line |
| 7C | `ruflo memory store -k init/hnsw-seed-{1,2,3} -v bootstrap` (×3 idempotent seeds) | Triggers the lazy HNSW index build (HNSW lives in-DB, not on filesystem — confirmed empirically) | Skipped if either `.swarm/hnsw.index` is non-empty **OR** `ruflo memory stats` reports `HNSW Index │ active` (handles both ASCII `\|` and box-drawing `│` separators) |
| 7D | `ruflo neural train -p coordination -e 50 --wasm --flash --contrastive` | SONA self-learning kicks off, seeds the pattern store and trajectory log | Skipped if `ruflo neural status` reports `Patterns: N` (>0) or `N patterns stored` (handles both number-after-noun and number-before-noun output formats) |
| 7E | `ruflo hooks pretrain -p . --depth shallow --with-embeddings` (depth overridable via `HOOKS_PRETRAIN_DEPTH`) | Pre-populates the hook routing pattern store so SendMessage / agent-spawn decisions have priors | Skipped if `ruflo hooks intelligence stats` reports `Patterns Learned │ N` (>0) — matches the box-drawing tabular output |
| 7F | `ruflo hive-mind init -t hierarchical-mesh -c byzantine -m 15 -p --memory-backend hybrid` | `.claude-flow/hive-mind/state.json` (collective memory + byzantine consensus + 15-agent capacity) | Skipped if `.claude-flow/hive-mind/state.json` exists |
| 7G | `ruflo daemon start` | The 5–12 background workers (audit, optimize, testgaps, map, document, etc.) that actually grow the system between user interactions | Skipped if `ruflo daemon status` matches `Status: ● RUNNING` (U+25CF filled circle, with `Status:.*RUNNING` regex fallback). On `--force` and currently RUNNING, the script stops the daemon first then re-seats it. |

**Order matters**: A → B → C → D → E → F → G. `embeddings init` (7B) must precede any HNSW-triggering store (7C). `neural train` (7D) and `hooks pretrain` (7E) require the embedding provider from 7B. `daemon start` (7G) must come last; otherwise the workers crash-loop on uninitialised swarm/hive-mind state.

**Fail-soft**: any sub-step that fails logs to `/tmp/ruflo-*-{init,build,start}.log` and the script continues. Partial activation is more useful than aborted activation; you can repair individual sub-steps from the log without losing the rest. The summary surfaces partial state.

**Why the script doesn't trust `$?` for daemon status**: `ruflo daemon status` always exits 0 regardless of whether the daemon is running or stopped. The status check uses byte-literal `grep` for `Status: ● RUNNING`.

### Flags

- `--force` — re-runs each step even if its skip condition is true (passes `--force` to `ruflo init` and `--upgrade` to `aqe init`). Implies `--reactivate` (superset).
- `--reactivate` — **lighter-touch refresh** (Tier 3). Skips steps 2/3/4 (`ruflo init`, `ruflo memory init`, `aqe init` — the expensive ones that take minutes), then runs steps 5/6/7/8/9 (`fix-ruflo.sh`, `fix-statusbar.sh`, the activation table with daemon re-seat, seed memory, verification). Use when the bootstrap was already done but something in the activation layer stopped working (daemon died, swarm aged out, embeddings provider unseated, etc.). `--force` and `--reactivate` are mutually-exclusive at the banner level — if both are passed, `--force` wins (banner shows only `--force`, and everything runs). The step 7G daemon re-seat fires under either flag.
- `--dry-run` — prints every action that would run, without modifying anything. Orthogonal to `--force` / `--reactivate`.
- `-h` / `--help` — shows the header comment.

### Exit codes

- `0` — all 8 steps either ran successfully or were skipped because already complete.
- `1` — at least one step reported an error (verification will show which).

### Typical first-run output

After a clean run on a fresh project you should see something like:

```text
Steps run:     8           # the 6 main steps + the seed + 7 activation sub-steps each counting
Steps skipped: 1           # only the seed when key already exists
Activation table: 7 run, 0 skipped
Verification:  10/10 checks passed
All systems ready.
```

After a re-run with no `--force`, every step should be skipped:

```text
Steps run:     2           # the two fix-* scripts, which are always idempotent and always run
Steps skipped: 11          # ruflo init, memory init, aqe init, the seed step, and all 7 activation sub-steps
Activation table: 0 run, 7 skipped
Verification:  9/9 checks passed
```

The "Activation table: 0 run, 7 skipped" line is the key idempotency signal — if any of 7A–7G shows as "run" on a re-run, the skip-check for that sub-step is broken (was the source of a previous bug — see "Local patches" item 7 if it regresses).

---

## `fix-ruflo.sh` — what each step does

The script prints `[N/11] Section` headers as it runs. Knowing what each step is for makes it easier to interpret the output and decide whether warnings matter.

| Step | Section | What it does | Why it matters |
|---|---|---|---|
| 1/11 | Environment | Locates `node` and `ruflo`. Aborts if Node is missing. | Establishes a baseline. |
| 2/11 | Stale global packages | Removes a stale `claude-flow` global (since renamed to `ruflo`); installs or updates `ruflo@latest` globally. | Two globals with overlapping bin entries silently shadow each other. |
| 3/11 | Shadow `agentdb` detection | Walks `~/node_modules`, the project's `node_modules`, and every `~/.npm/_npx/*/node_modules/agentdb`. Removes top-level orphans whose major version is `< 3` (bundled deps under `@claude-flow/*/node_modules/` are kept). | A v1/v2 `agentdb` anywhere up the resolution chain resolves bare `import('agentdb')` to the wrong package — disables every controller. |
| 4/11 | Local ruflo dependency check | Removes any local `package.json` dependency on `ruflo`, `@claude-flow/cli`, or `@claude-flow/memory`. | Local deps override the MCP-managed runtime and cause version drift. |
| 5/11 | MCP configuration | Recognizes the canonical claude-flow launch as the **global** `ruflo mcp start` (command:`ruflo`, NOT `npx -y ruflo@latest` — Tier 7/Patch 18); warns if an npx-form entry is found; creates the file from a global-launch template if missing. Validates `.claude/settings.json` for `statusLine` + `hooks`. | Claude Code reads `.mcp.json` at boot; npx reverts the agentdb controller fix on every call, so the MCP must launch from the global install. |
| 6/11 | Claude MCP registration | Re-registers the MCP server in Claude (`claude mcp add claude-flow -- ruflo mcp start`) if Claude CLI is installed; treats npx-form registrations as stale and migrates them to the global launch. | Skips silently if `claude` is not on `PATH`. |
| 7/11 | NPX cache cleanup | Deletes `~/.npm/_npx/*` subtrees that have `@claude-flow/memory` without the `controller-registry.js` (broken builds) or that bundle the old `claude-flow` without `ruflo`. | Stale npx caches mask fresh package resolutions. |
| 8/11 | State file cleanup | Resets `.claude-flow/agents.json` if it exceeds 10 KB ("ghost agents"); resets `.claude-flow/hive-mind/state.json` if it lists > 20 ghost workers; resets `swarm-state.json` if a swarm has been marked "running" with 0 agents for more than 30 minutes. | Long-running sessions accumulate phantom workers that confuse the swarm. |
| 9/11 | NVM multi-version check | Reports each installed Node version whose `ruflo` is not the latest. Does **not** auto-update them. | If you switch Node versions, the wrong ruflo can take over. |
| 10/11 | Agentic QE plugin | **Advisory only (Tier 7).** Detects the legacy `@claude-flow/plugin-agentic-qe` and, if present, prints migration guidance — it does NOT install, update, or uninstall it, and does NOT count it as an error. Absence is the correct state. | The standalone `agentic-qe` package (60 agents, 13 contexts) is the successor. Older versions of this step *installed* the legacy plugin, which fought fix-statusbar.sh Step 4 (which removes it) — an install/uninstall ping-pong + spurious exit 1, now removed. |
| 10b | AgentDB / ReasoningBank patches | Patch 0 (`require('path')` → `import('node:path')`) and Patch 2 (add `embedder` to the `ReasoningBank` ctor) remain as **idempotent dormant guards** — `@claude-flow/memory@alpha.18` already satisfies both. **Patch 1 (bare `import('agentdb')` rewrite) is RETIRED (Tier 7):** the real controller fix is **Step 3b**, which force-pins the GLOBAL tree's nested `agentdb` to `3.0.0-alpha.10` so the bare import resolves to the controller-bearing version (alpha.14 regressed those classes). See Patch 18. | Without alpha.10 in the global tree, 7 advanced controllers (`mutationGuard`, `gnnService`, `attestationLog`, `semanticRouter`, `guardedVectorBackend`, `rvfOptimizer`, `graphAdapter`) stay dormant. |
| 11/11 | Statusline patches | Adds plugin detection, swarm-stale threshold (5 min → 30 min), ghost-swarm detection, subagent detection from stdin + JSONL scan, richer swarm-line render. All patches are inserted into the existing `statusline.cjs` and detected on re-run. | Without this the status line goes stale or shows ghost data. |

The script ends with a **Summary** listing every fix applied and any unresolved manual actions. It tees full output to `/tmp/fix-ruflo-YYYYMMDD-HHMMSS.log`.

### Verifying after `fix-ruflo.sh`

Restart Claude Code, then run these MCP queries inside Claude:

```text
mcp__claude-flow__agentdb_health
# Expect: agentdb: connected, version 3.x, controllers >= 15

mcp__claude-flow__memory_store --key test --value ok
mcp__claude-flow__memory_search --query test
# Expect: returns the entry you just stored

mcp__claude-flow__hive-mind_status
# Expect: clean state (no ghost workers from previous sessions)
```

If `agentdb_controllers` shows fewer than **23/23 active** after a restart (the 7 advanced controllers — `mutationGuard`, `gnnService`, `attestationLog`, `semanticRouter`, `guardedVectorBackend`, `rvfOptimizer`, `graphAdapter` — dormant), the GLOBAL tree's nested agentdb is not pinned to `3.0.0-alpha.10` (Tier 7/Patch 18). Re-force it in the global install (npx caches revert and are NOT the launch path):

```bash
bin/ruflo-kit fix-ruflo <target>        # Step 3b force-pins the global agentdb to alpha.10
# restart Claude Code (the running MCP holds the old agentdb in its ESM cache)
```

---

## `fix-statusbar.sh` — what it does

After `aqe init` (or any tool that resets `.claude/helpers/`), the rich Claude Code status line is often replaced by a minimal stub. This script restores the dual ruflo + Agentic QE v3 display in four steps:

Before any patching, the script **detects the installed ruflo version dynamically** by running `ruflo --version` (falling back to `npx -y ruflo@latest --version`) and extracting the `<major>.<minor>(.<patch>)?` from the output. It then exposes two values to the node-side patcher: `RUFLO_VERSION_LABEL` (e.g. `V3.10`, used for any hardcoded header strings in older templates) and `RUFLO_VERSION_FULL` (e.g. `3.10.3`, used to rewrite the runtime-fallback default). If detection fails completely it defaults to `V3` / `3` so the script doesn't crash on offline machines.

1. **Patches `.claude/helpers/statusline.cjs`**:
   - **(1a)** Bumps any literal `▊ RuFlo V<X>[.<Y>] ` header to the detected label (no-op on modern templates, which concatenate the version at runtime).
   - **(1b)** Rewrites the hardcoded runtime fallback default `let pkgVersion = '3.X';` (which goes stale across ruflo releases) to the detected `<major>.<minor>.<patch>`. This is the patch that actually matters on the current template — it ensures the rendered header shows the right number when ruflo isn't installed in any of the four `package.json` lookup paths the renderer checks.
   - **(1c)** Injects a `getAQEStats()` helper that reads `.agentic-qe/memory.db` via the `sqlite3` CLI (avoiding `better-sqlite3` ABI drift), and inserts a "Line 6: Agentic QE v3 footer" render block.
   - **(1d) — `node --check` post-write guard.** Before any patching, the script snapshots `statusline.cjs` to `.bak`. After writing, it runs `node --check "$STATUSLINE_FILE"`; on syntax-error failure it restores from the `.bak` so the file is never left in a broken state. On success the `.bak` is removed. Note: the `.bak` is only useful if the source was clean when the snapshot was taken — pre-existing corruption is repaired by step 1e's auto-repair branch (the `///g` case) rather than via `.bak` rollback. (Known limitation: arbitrary unknown corruption introduced outside the script's vocabulary will leave `.bak` containing the same corruption — defense-in-depth, not a complete recovery mechanism.)
   - **(1e) — stdin-header verify-and-guard.** Modern ruflo `statusline.cjs` ships with stdin renderers for model / cost / context-% / session-uptime (these are what populate the `Opus 4.7 │ ⏱ 46m25s │ ● 32% ctx │ $21.97` segment of the header). This step greps for **four anchor patterns** in `statusline.cjs`: `getStdinData()`, `data.model.display_name`, `data.cost.total_cost_usd`, `data.context_window.used_percentage`. If all four are present, logs `pass: stdin header fields wired (4/4)` and skips. If any are missing, logs a warning naming the missing anchors and tells the user to re-run `npx ruflo init --force` — it does **not** auto-inject a renderer, because a future ruflo upgrade may have customised the file in ways the patcher doesn't understand, and warning loudly is safer than writing broken code.
   - **(1f) — auto-repair** Detects a known-bad pattern from prior gist versions: `CWD.replace(///g, '-')` (where the slash-escaping was double-stripped by bash + JS template literal and `//` ended up parsed as a single-line comment, crashing the file with `SyntaxError: Unexpected token 'const'`). If found, repairs it to `CWD.replace(/\\//g, '-')`. The upstream `fix-ruflo.sh` step that originally introduced this corruption has been patched locally too (see "Local patches").
   - **(1g) — MCP counter rewrite (sentinel `MCP-COUNT-PATCH-V1`)** Rewrites `getIntegrationStatus()` in `statusline.cjs` so the `MCP <N>/<M>` chip combines two distinct server scopes correctly. User-scope servers in `settings.json` `mcpServers` are always counted as enabled. Project-scope servers in `.mcp.json` are filtered through `settings.json` `enabledMcpjsonServers` (the Claude Code allowlist that only applies to the `.mcp.json` set, never to the user-scope set). `total` = deduped union of both scopes; `enabled` = user-scope count + filtered project-scope count. Before this patch, `enabledMcpjsonServers` was being applied to the wrong server set — on a project with `mcpServers: {ruflo}` and `enabledMcpjsonServers: ['agentic-qe']`, the filter produced an empty intersection and the chip rendered `●0/1` red despite the MCP server being healthy. The patch is gated by the existing 1d `node --check` + `.bak` rollback guard, so any future syntax-breaking edit auto-restores.
   - **(1h) — Sqlite-aware Vectors + HNSW probe (sentinel `VECTORS-HNSW-PATCH-V1`)** Rewrites `getAgentDBStats()` in `statusline.cjs` to count real vector-bearing rows via `sqlite3 -readonly` probes of `.swarm/memory.db` (ruflo: sums `memory_entries WHERE embedding IS NOT NULL` + `pattern_embeddings` + `learning_state_embeddings`) and `.agentic-qe/memory.db` (AQE: sums `embeddings` + `qe_pattern_embeddings`). Treats a non-empty `vector_indexes` table as the in-DB HNSW-active signal — required because HNSW indexes are stored as DB rows, not as a `.swarm/hnsw.index` file (lazy in-DB build; see Local Patch 7's 7C skip-rule for the same pattern). The HNSW-chip on the DDD-domains line then renders `⚡ HNSW <N>x` from a vectorCount-keyed heuristic ladder (`<1000 → 10x`, `≥1000 → 150x`, `≥10000 → 12500x`), replacing the prior misleading `📚 <N> patterns` chip whose count was an SQLite page-count artefact rather than a real pattern count. **`-readonly` mode is load-bearing** — concurrent statusline renders during a live DB write would otherwise risk corruption. The patch reuses `safeStat` / `safeExec` helpers already in `statusline.cjs`; idempotent via the sentinel comment.
   - **(1i) — Subagent file-scan counter (sentinel `SUBAGENTS-SCAN-V2`)** Rewrites the `// Sub-agents:` block in `statusline.cjs` (originally written by `fix-ruflo.sh` step 11d as a sentinel-less V1) to fix two compounding bugs that left `Sub` stuck at 0. (1) The project-key derivation `CWD.replace(/\//g, '-')` only mangled `/`, but Claude Code's `~/.claude/projects/<key>/` directory mangles **both** `/` and `_` to `-` — projects with underscores in their path (e.g. `THE_AI`) silently missed their CC directory. New regex is `CWD.replace(/[\/_]/g, '-')`. (2) The real jsonl layout is `<projDir>/<key>/<sessionUUID>/subagents/*.jsonl`, not `<projDir>/<key>/subagents/*.jsonl` — old code looked one level too shallow. New code recurses into per-session directories. The substring filter (`d.includes(projKey) || projKey.includes(d)`) is also tightened to exact equality (`d === projKey`) — post-regex-fix the key is exact, so substring would only allow cross-project leakage. 10-minute mtime threshold preserved (right liveness signal). Anchored on the existing `// Sub-agents: prefer Claude Code stdin data, fallback to file scan` comment and the trailing `return { memoryMB, contextPct, intelligencePct, subAgents };`; protected by the existing step 1d `.bak` + `node --check` guard.

#### What the swarm-row chips count

The "Swarm" line on the statusline shows two independent counters:

- **`[N/15]`** (the `🤖 Swarm  ◯ [N/15]` chip) — counts **ruflo-domain agents**, those spawned via `ruflo agent_spawn`, `mcp__claude-flow__swarm_init` with an `agents: [...]` list, or `ruflo swarm spawn`. This is the hive-mind / coordination worker count, capped at `CONFIG.maxAgents` (default 15).
- **`Sub N`** (the `👥 Sub N` chip) — counts **Claude Code's `Agent`-tool background subagents**, i.e. anything spawned via `Agent({ run_in_background: true })` from inside Claude Code. The source of truth is the per-session `~/.claude/projects/<key>/<sessionUUID>/subagents/*.jsonl` files with `mtime < 10 min`.

These are independent. Running Claude Code `Agent` calls does NOT increment `[N/15]`, and running `ruflo agent_spawn` does NOT increment `Sub N`. If you've launched a coder/tester/reviewer pipeline via `Agent({ run_in_background: true })`, watch `Sub N`. If you're driving ruflo's hive-mind workers, watch `[N/15]`.

2. **Writes a dual fallback `.claude/helpers/statusline-v3.cjs`** — a self-contained single-line renderer that reads both `.swarm/memory.db` (ruflo) and `.agentic-qe/memory.db` (Agentic QE) directly via `sqlite3`. Survives any future `aqe init` because Claude Code's `statusLine.command` falls back to this if `statusline.cjs` crashes.
3. **Patches `.claude/settings.json`** — sets `statusLine.command` to `node statusline.cjs 2>/dev/null || node statusline-v3.cjs 2>/dev/null || echo "▊ RuFlo + Agentic QE v3"`.
4. **Removes the legacy `@claude-flow/plugin-agentic-qe`** if it is in `package.json` or `node_modules` (it has been superseded by the standalone `agentic-qe` package).

The script ends by rendering the status line to stdout so you can see the result without restarting Claude Code. If the rich render fails, it falls back to the dual stub and warns — that means `statusline.cjs` itself has an unrelated issue; check `node .claude/helpers/statusline.cjs` directly to see the stack trace.

Restart Claude Code (Ctrl+C, `claude`) for the new status line to appear in the chrome.

---

## `ruflo-session-init.sh` — the daily entry point

Seven-step verification that wraps `fix-ruflo.sh` + `fix-statusbar.sh` and adds runtime checks:

1. **Applies ruflo patches** by calling `fix-ruflo.sh` (silent — only counts the ✓ checks).
2. **Repairs the status bar** by calling `fix-statusbar.sh` (silent).
3. **Confirms the MCP server is running** by looking for `ruflo mcp start` in `ps aux`.
4. **Daemon auto-start** (NEW). Greps `ruflo daemon status` for `Status: ● RUNNING`. If the daemon died between sessions (the common case — daemons don't survive a machine reboot or a Claude Code restart), runs `ruflo daemon start` and logs to `/tmp/ruflo-session-daemon-start.log`. Idempotent — a second run reports "daemon already running". This single step closed the most common cause of the loop silently stopping between sessions: the bootstrap left the daemon RUNNING, but the next time the user opens Claude Code, no one had restarted it.
5. **Inspects persistent storage** — sizes of `.swarm/memory.db`, `.swarm/hnsw.index`, `.claude-flow/data/graph-state.json`, `.claude-flow/data/intelligence-snapshot.json`.
6. **Verifies AgentDB controllers** — re-checks that the three `controller-registry.js` patches are still in place (catches the case where a cache wipe undid them).
7. **Checks RuVector native binaries** — `sona.<arch>.node`, `attention.<arch>.node`, `ruvector-graph*.node` for the host's architecture (`darwin-arm64`, `darwin-x64`, etc.).

Final summary lists how many issues were found, and ends with the three MCP queries to run inside Claude Code to confirm `agentdb_controllers` is healthy, `memory_search` returns project data, and the intelligence layer is reachable.

**Recommended habit:** add the line below to your project's CLAUDE.md or session bootstrap notes:

```text
At the start of each session, run:  bin/ruflo-kit session <target>
```

---

## `upgrade-2026-05-26.sh` — Phase-A major-version migration

A one-shot script for the ruflo 3.6.x → 3.10.x bump (the version numbers are illustrative — the script reads `npm view ruflo version` at runtime). It is **not idempotent** in the sense that it deletes the whole `~/.npm/_npx` cache; running it twice in a row is harmless but wasteful.

Sequence (8 steps, renumbered in Tier 3 to bracket the wipe with daemon lifecycle):

1. **Pre-flight** — Node version, current vs latest ruflo, warn if `ruflo mcp start` is currently running.
2. **Daemon stop** (Tier 3, NEW) — `ruflo daemon stop || true` to release any open file handles the running daemon holds into the npx cache. Tolerant of both running and already-stopped states (`ruflo daemon stop` always exits 0). Without this step, the rm -rf in step 5 can race against an in-flight daemon worker, leaving the next session's `daemon start` to fail silently.
3. **Snapshot** — record `ls ~/.npm/_npx > /tmp/ruflo-npx-snapshot-*.list` so you can re-resolve other tools if you depended on a cached version.
4. **Upgrade** — `npm install -g ruflo@latest --foreground-scripts` (the `--foreground-scripts` flag works around [npm GH#1825](https://github.com/npm/cli/issues/1825) which silently fails on Node 22/25).
5. **Wipe** — `rm -rf ~/.npm/_npx` so the next `npx` resolves cleanly.
6. **Rehydrate** — `npx -y ruflo@latest --version` to pull a fresh tree of `@claude-flow/memory@alpha.N`, `agentdb@alpha.M`, etc.
7. **Re-apply patches** — runs `fix-ruflo.sh`. Patches 0 + 2 report "already present" on alpha.18; **Step 3b** force-pins the GLOBAL tree's `agentdb` to alpha.10 (the upgrade's `npm i -g ruflo` at step 4 reset it to ^alpha.14). Patch 1 is retired (Tier 7).
8. **Re-activate** (Tier 3, NEW) — runs `bin/ruflo-kit init <target> --reactivate`. This re-seats the daemon, swarm, embeddings, neural, hooks, and hive-mind layers against the new code paths. Skips the expensive `ruflo init` / `aqe init` (steps 2/3/4 of the bootstrap) — those don't need to re-run after an upgrade because the project files on disk don't change. Without this step, the user is told to "restart Claude Code", which works for the MCP server (lazy-spawned) but NOT for the daemon or swarm state (which need explicit re-seating to bind against the new bundle).

### Before running

- **Close Claude Code first** (the live MCP server holds open handles into the cache).
- Read the project's `docs/UPGRADE-PATH-*.md` if it exists — there may be project-specific migration notes (`agentic-qe` version pins, plugin removals, etc.).

### Rollback

If something breaks after the upgrade, the script prints the rollback command in its final summary:

```bash
npm install -g ruflo@<previous-version>
# npx caches auto-rehydrate; nothing else to restore
```

---

## `ruflo-health.sh` — growth-delta monitor (Tier 3, NEW)

The other scripts in this kit answer "did init run?" and "are the patches applied?". This one answers a harder question: **"is the loop actually growing?"** It captures the metrics the statusbar shows (memory entries, SONA patterns/trajectories, MoE accuracy, ReasoningBank patterns, DB rows/sizes, daemon status, hive workers) and compares against the previous run's snapshot to surface growth, flatlines, and regressions.

### What gets tracked

Seven metric groups, all probes are cheap (~1.5s total, no ONNX inference):

| Group | Metrics | Source | Cost |
|---|---|---|---|
| Memory | totalEntries, hnswEntries | `ruflo memory stats` | ~350ms |
| Intelligence (SONA + MoE) | sonaPatterns, sonaTrajectories, sonaQualityPct, moeRoutingAccuracyPct, searchSpeedupLabel | `ruflo hooks intelligence stats` | ~210ms |
| Neural | reasoningBankPatterns | `ruflo neural status` | ~310ms |
| DB rows | swarmMemoryEntries, swarmPatternEmbeddings, swarmVectorIndexes, aqePatterns, aqeTrajectories | `sqlite3 -readonly` probes on `.swarm/memory.db` + `.agentic-qe/memory.db` | ~20ms each |
| DB sizes | swarm (bytes), aqe (bytes) | `stat -f %z` | <5ms |
| Runtime | daemon.running | `ruflo daemon status` (grep RUNNING — `$?` is unreliable, see Patch 7's daemon-status notes) | ~200ms |
| Hive | workers | parsed from `.claude-flow/hive-mind/state.json` | ~5ms |

**Intentionally NOT used**: `ruflo embeddings index` (Tier 2 lesson — ONNX inference per call is too expensive) and `aqe status` (noisy `[INFO]` chatter from re-init on every call; equivalent info comes from the sqlite probes).

### Files written

- `.claude-flow/data/health-last.json` — most recent snapshot (overwritten each run unless `--dry-run`). Schema version 1; future revisions auto-detect version mismatch and treat as a fresh baseline rather than producing nonsense diffs.
- `.claude-flow/data/health-history.jsonl` — append-only JSONL history. One line per run. Useful for plotting growth-over-time with a one-liner like `jq -s 'map({iso, sonaPatterns: .metrics.intelligence.sonaPatterns})' .claude-flow/data/health-history.jsonl`.

### Output format

Color-coded delta lines per metric:
- `↑` brightGreen — growth (`prev → curr (+delta)`)
- `↓` brightRed — regression (only triggers exit-1 on monotonic metrics; DB sizes can shrink on cleanup without warning)
- `✓` dim — stable AND fresh (<24h since baseline)
- `!` brightYellow — flatlined (no change AND >24h since baseline AND metric is in the growth class) — likely daemon died or hook misfiring
- `─` dim — label-only metric (e.g. `searchSpeedupLabel: 150x` is a static upstream constant, not a measurement — see Tier 2 item 6 lesson)

Ends with a Summary line (`N grew  M stable  K warned  J regressed`) and a Verdict (`✓ healthy growth — learning loop active` | `✓ all stable (no growth, no regression)` | `! partial — some metrics flatlined, check daemon` | `✗ regression detected — investigate`).

### CI-friendly exit code contract

- **exit 0** — healthy growth, all stable, or partial (with warnings).
- **exit 1** — at least one growth-class metric regressed. Independently verified by injecting `sonaPatterns=999` into the snapshot and confirming the script exits 1. The contract is: if `EXIT=1`, something monotonic went backwards and a human should look.
- **`set -o pipefail` caveat for CI authors**: if you pipe the output through `tail` / `grep` / etc. for log truncation, use `set -o pipefail` (or capture `${PIPESTATUS[0]}`) — otherwise the exit code reflects the pipeline's last command (likely 0), masking the regression signal.

### CLI

```bash
bin/ruflo-kit health <target>                # capture current metrics, diff against last snapshot, then update the snapshot
bin/ruflo-kit health <target> --reset        # force re-baseline (overwrite snapshot — use after a manual DB wipe)
bin/ruflo-kit health <target> --dry-run      # diff against last snapshot but do NOT update it — for passive monitoring in cron/CI
bin/ruflo-kit health <target> --json         # emit raw current snapshot JSON only (suppresses the rendered diff)
bin/ruflo-kit health <target> -h             # help text
```

Auto-detect first-run: if `.claude-flow/data/health-last.json` doesn't exist or has a version mismatch, the script writes a baseline + exits 0 with `baseline established — re-run later`. No separate `--init` flag needed.

### Typical usage

- **Per-session at the end**: `bin/ruflo-kit health <target>` to confirm the session's agent activity produced measurable learning growth. Pairs well with `bin/ruflo-kit session <target>` at the start.
- **Cron / CI gating**: `set -o pipefail; bin/ruflo-kit health <target> --dry-run` — fails the build if any growth-class metric regressed, doesn't mutate state. Add to `.github/workflows/` or a cron entry that emails on non-zero exit.
- **Post-upgrade verification**: `bin/ruflo-kit upgrade <target> && bin/ruflo-kit health <target> --reset` — re-baseline after a major upgrade so post-upgrade growth deltas are measured against the new code's behavior, not the old.

---

## Local patches (divergence from upstream gist)

The copies of these scripts in `lib/` have been patched locally to fix seventeen issues present in the upstream gist as of 2026-05-27 — three in bash escaping, one in the hardcoded version, one in the memory-seed UNIQUE constraint handling, one in the AQE init flow that left projects in a half-initialized state, two structural gaps (missing activation table, missing daemon auto-start) that left the self-learning loop dormant after bootstrap, two statusline rendering bugs (wrong MCP scope filter, file-based vector count instead of sqlite probe) that surfaced after Tier 1 activated the full stack, one defense-in-depth gap (no `node --check` guard on `fix-ruflo.sh` step 11's four `node -e` patching blocks), one statusline accuracy bug (`Sub N` subagent counter stuck at 0 due to two compounding bugs in the file-scan fallback), three statusline-chip dishonesty bugs uncovered by the Tier 4 audit (Vectors undercount, `💾 MB` chip measuring the renderer's own V8 heap, `🧠 %` chip derived from a SQLite page-count heuristic), one statusline-domain bug uncovered by the Tier 5 audit (the AgentDB chip's Vectors/Size counters mixed ruflo + AQE state, and the AQE footer surfaced no vector or DB-size signal at all), and five Tier 6 operational + diagnostic gaps (missing agentdb MCP server registration; absent `--force` cleanup of the on-disk hnsw.index sidecar + no post-build invariant check; transient orphan-agentdb warning that didn't explain the upstream re-hoist cycle; missing ONNX-model cache preservation across npx wipes; no visibility into whether HNSW search is using the fast NAPI binary or the silent WASM fallback). Re-downloading from the gist will reintroduce these issues.

1. **`grep -c … 2>/dev/null || echo 0`** (fix-ruflo.sh:786,789,802 ; ruflo-session-init.sh:35,47,121–124) — `grep -c` prints `0` on no matches **and** exits non-zero, so `|| echo 0` doubled the output to `"0\n0"`, which then broke `[[ "$VAR" -gt 0 ]]` arithmetic with `syntax error in expression`. Replaced with `|| true` and a `${VAR:-0}` fallback line.
2. **`'\\$1'` inside a `node -e "…"` double-quoted string** (fix-ruflo.sh:945) — bash tried to expand `$1` (the script's first positional arg); under `set -u` this aborted the step with `$1: unbound variable`. Replaced with `'\$1'` so bash forwards a literal `$1` to JS, where it becomes the regex backreference.
3. **Slash regex corrupted by bash + JS template literal double-stripping** (fix-ruflo.sh:1083, statusline patch step 11d) — source had `CWD.replace(/\\//g, '-')`. Bash collapsed `\\` → `\`, then the surrounding JS template literal dropped the `\` before `/` (because `/` isn't a JS string escape), so the file got written as `CWD.replace(///g, '-')`. The `//` then parsed as a single-line comment, breaking the file with `SyntaxError: Unexpected token 'const'` on the next line — the rich statusline died and Claude Code silently fell back to the stub. Source now uses `\\\\//g` (four backslashes) so the chain becomes `\\\\` → `\\` (bash) → `\/` (template literal) → final JS `/\//g`. `fix-statusbar.sh` also gained an auto-repair branch (step 1d) that detects the corrupt pattern and rewrites it, so users with the broken file from a prior gist version heal automatically on the next session-init.
4. **Hardcoded ruflo version label** (fix-statusbar.sh) — `▊ RuFlo V3.10` was a literal in the patcher, and the underlying `statusline.cjs` had a stale `let pkgVersion = '3.6';` runtime fallback. Both now derive from `ruflo --version` (or `npx -y ruflo@latest --version` as a fallback) at script-run time. Two values are exposed: `RUFLO_VERSION_LABEL` (e.g. `V3.10`, for the legacy literal-header branch) and `RUFLO_VERSION_FULL` (e.g. `3.10.3`, for the runtime fallback). Both are inserted only if they differ from what's already on disk, keeping the patch idempotent.
5. **Memory-seed `UNIQUE` constraint on re-run** (init-ruflo-aqe-agentdb.sh, step 7) — the original `ruflo memory store -k init …` would always fail on re-runs because ruflo's SQLite-backed memory enforces `UNIQUE(namespace, key)`, and the failure mode wasn't transient so no retry helped. Step 7 now does a `ruflo memory retrieve -k init` pre-check first; if the key exists, it's counted as a skipped step. If missing, store runs under an exponential-backoff retry (1s / 2s / 4s / 8s, 5 attempts) with a race-check between attempts that treats a concurrent insert as success.
6. **AQE half-init detection and repair** (init-ruflo-aqe-agentdb.sh, step 4) — the original script ran `npx -y agentic-qe@latest init --memory hybrid`, which only initializes the AQE SDK (`.agentic-qe/memory.db`, SONA engine, DreamScheduler, Queen Coordinator) but **does not** copy the project-level integration (~115 `.claude/skills/`, AQE agents, `aqe-*.md` commands, the `## Agentic QE v3` section in `CLAUDE.md`). The skip-check then saw `.agentic-qe/memory.db` and concluded "already initialized", silently leaving the project in a half-init state where AQE's MCP tools work but its hooks, skills, and policy rules don't load into Claude Code.
   - **Fix part A** — switched to `aqe init --auto` (the canonical first-time flag documented in the [agentic-qe README](https://github.com/proffesor-for-testing/agentic-qe#quick-start)), which performs both SDK init and project integration in one shot.
   - **Fix part B** — prereq check (step 1) now detects globally-installed `aqe` (e.g. `aqe 3.10.1`) and prefers it over `npx -y agentic-qe@latest`. The npx path resolves a fresh package per run on machines without `aqe` on `PATH` — slower and a frequent source of stale-cache drift.
   - **Fix part C** — skip-check now requires **both** `.agentic-qe/memory.db` **and** an AQE-only marker (`.claude/skills/agentic-quality-engineering`, present only after `aqe init --auto`). If the DB exists but the marker is missing, the script identifies it as a half-init and runs `aqe init --auto --upgrade` automatically to complete it.
   - **Fix part D** — added a 9th verification check (step 8) for the same marker, so any future half-init from an external tool surfaces immediately rather than hiding behind the 8-check green.

   This was the bug that motivated the cross-check: on this project, the original `--memory hybrid` invocation produced 30 skills (ruflo only) and 6 agents. After switching to `aqe init --auto`, the project gained **+84 skills (now 114 total)**, 13 of them AQE-specific (`qe-test-generation`, `qe-coverage-analysis`, `qe-quality-assessment`, `qe-chaos-resilience`, `agentic-quality-engineering`, etc.), plus the AQE policy section in `CLAUDE.md`. The presence of those skills is also why `UserPromptSubmit` hooks now return live AQE routing data (recommended agents, bounded-context domains, guidance lines) — none of which fires when AQE is half-initialized.
7. **No activation table** (init-ruflo-aqe-agentdb.sh, before Tier 1) — the original bootstrap ran `ruflo init` + `ruflo memory init` + `aqe init --auto` and stopped. It never ran the activation sequence that turns "files on disk" into "self-learning loop active": `swarm init`, `embeddings init`, the HNSW-seed stores, `neural train`, `hooks pretrain`, `hive-mind init`, `daemon start`. Result: every metric on the statusline started at 0, the daemon was never running (so the background workers never produced new patterns), and the project stayed silently un-activated. Now: step 7/9 runs the 7-sub-step activation table (7A–7G) with proper ordering, idempotent skip-checks, `--dry-run` honouring, and `--force` cascade-into-daemon-stop-and-restart. Skip-checks for 7C/7D/7E required three regex iterations during implementation:
   - **7C** (HNSW): can't rely on `.swarm/hnsw.index` existing on disk (lazy build — HNSW lives in-DB). Skip-check accepts either the on-disk file OR `ruflo memory stats` reporting `HNSW Index │ active`.
   - **7D** (neural): `ruflo neural status` emits `N patterns stored` (number BEFORE noun), not `Patterns: N` (number AFTER noun). Skip-check accepts either format via regex alternation.
   - **7E** (hooks): `ruflo hooks intelligence stats` uses tabular output `│ Patterns Learned │ N │`. Skip-check matches the tabular cell with both ASCII `|` and box-drawing `│` separators.
8. **Daemon dies between sessions, never auto-restarted** (ruflo-session-init.sh, before Tier 1) — the bootstrap left the daemon RUNNING, but daemons don't survive a machine reboot or a Claude Code restart. Before Tier 1, the user had to manually `ruflo daemon start` every session or the self-learning loop silently stopped (vectors flatline, patterns don't grow, statusline numbers don't climb). Now: step 4/7 of session-init greps `ruflo daemon status` for `Status: ● RUNNING`; if not running, auto-starts and logs. Idempotent.
9. **MCP chip rendered `●0/1` red despite healthy server** (statusline.cjs `getIntegrationStatus()`, fixed via fix-statusbar.sh step 1g) — long-standing logic bug exposed by Tier 1's AQE half-init fix. `aqe init --auto` writes `enabledMcpjsonServers: ['agentic-qe']` into `.claude/settings.json` (this allowlist gates **project-scope** servers in `.mcp.json`). The original `getIntegrationStatus()` applied that allowlist to the **user-scope** `settings.json` `mcpServers` set, producing an empty intersection (`['agentic-qe'] ∩ ['ruflo']` = ∅) and rendering `●0/1` red. Now: user-scope servers count as always-enabled; project-scope servers are filtered through `enabledMcpjsonServers`; chip shows `enabled / total` across both scopes. On this project: 1 user-scope (`ruflo`, always on) + 2 project-scope (`claude-flow`, `agentic-qe` — only the latter in `enabledMcpjsonServers`) → `MCP ●2/3` yellow, which is the correct healthy render. Sentinel comment `MCP-COUNT-PATCH-V1`. This bug had been latent in `statusline.cjs` for an unknown number of versions; it only became visible once `aqe init --auto` started writing the allowlist. **Where the fix lives**: `lib/fix-statusbar.sh` step 1g (NOT a direct edit to `statusline.cjs`, because that file is clobbered by `aqe init` and `ruflo init --force`; the patch in `fix-statusbar.sh` re-applies on every session-init).
10. **Vectors `●0` + dishonest `📚 N patterns` chip** (statusline.cjs `getAgentDBStats()`, fixed via fix-statusbar.sh step 1h) — `getAgentDBStats()` previously read JSON files under `.claude-flow/data/` and `.claude-flow/memory/` (likely a stale path from a pre-rename layout — none of those files exist on a v3 ruflo install) and never queried the actual sqlite store at `.swarm/memory.db`. Result: `Vectors` flatlined at 0 even with vectorised entries on disk. The HNSW indicator likewise checked for an on-disk `.swarm/hnsw.index` file, which never exists under the lazy in-DB build pattern (HNSW indexes are rows in the `vector_indexes` table — same lazy pattern documented in Local Patch 7's 7C skip-rule). The `📚 N patterns` chip on the DDD-domains line was driven from an SQLite page-count heuristic in `getLearningStats()` (page-count minus a 2-page schema offset), which has nothing to do with real pattern counts — verified by `SELECT COUNT(*) FROM patterns` returning 0 while the chip displayed `48 patterns`. Now: `sqlite3 -readonly` probes both `.swarm/memory.db` (sums `memory_entries WHERE embedding IS NOT NULL`, `pattern_embeddings`, `learning_state_embeddings`) and `.agentic-qe/memory.db` (sums `embeddings`, `qe_pattern_embeddings`) for the true vector count. Non-empty `vector_indexes` flips `hasHnsw=true`. Chip renders `⚡ HNSW <N>x` from a vectorCount-keyed heuristic ladder when HNSW is active, instead of the misleading patterns count. On this project: `Vectors ●9⚡` (6 from `memory_entries` + 3 from `qe_pattern_embeddings`), `⚡ HNSW 10x` chip. Sentinel `VECTORS-HNSW-PATCH-V1`. **The `-readonly` flag is load-bearing**: without it, concurrent statusline renders during a live DB write could risk corruption.

11. **No `node --check` guard on `fix-ruflo.sh` step 11's `node -e` patching blocks** (fix-ruflo.sh:895–898, 1200–1213) — step 11 has four `node -e` blocks (11a plugin detection, 11c ghost-swarm + topology, 11d subagent detection, 11e swarm-line render), each one a candidate for the same class of bash + JS template-literal escape bug that produced the `///g` corruption (see Patch 3). `fix-statusbar.sh` step 1d has had a post-write `node --check` + `.bak` rollback guard since Tier 1.2; `fix-ruflo.sh` did not. Now: a single guard at step-level (not per-sub-patch — sub-patches are additive on the same file, so a broken intermediate write would cascade into the next sub-patch's input, and on rollback we want to bail the whole step). Snapshot to a distinct `${STATUSLINE_FILE}.fixruflo.bak` suffix (avoids stomping fix-statusbar.sh's own `.bak` on back-to-back runs). On `node --check` failure: `mv` restores the snapshot, the stderr capture is teed to the user, `ERRORS++` increments so CI sees the failure surface non-zero. On success: `rm -f` cleans up the `.bak`. Idempotent — the existing grep-based marker checks per sub-patch are unchanged, so a re-run after rollback re-applies cleanly.

12. **Subagent counter stuck at `Sub 0` despite active background subagents** (statusline.cjs `// Sub-agents:` block, originally at lines 415–446; fixed via fix-statusbar.sh step 1i with sentinel `SUBAGENTS-SCAN-V2`) — two compounding bugs in the file-scan fallback left the chip at 0 even with 3–4 active `Agent`-tool background subagents writing live jsonls: **(A) project-key derivation mismatch.** The old `CWD.replace(/\//g, '-')` regex only mangled `/`, but Claude Code's actual derivation for `~/.claude/projects/<key>/` mangles **both** `/` AND `_` to `-`. Any project path containing an underscore (e.g. `/Users/cm/THE_AI/...`) silently failed to match its CC directory (`-Users-cm-THE-AI-...`, NOT `-Users-cm-THE_AI-...`). The substring fallback `d.includes(projKey) || projKey.includes(d)` didn't rescue it because the mismatch is mid-string. **(B) wrong path depth.** The old code looked at `<projDir>/<key>/subagents/*.jsonl`, but the real layout puts a session UUID in between: `<projDir>/<key>/<sessionUUID>/subagents/*.jsonl`. The shallow `subDir` never exists → loop skips → count stays 0. Now: `[\/_]` regex matches Claude Code's full path-mangling rules; the scan recurses one level into per-session directories; and the substring filter is tightened to exact equality (`d === projKey`) since the corrected derivation produces an exact key (substring would only allow cross-project leakage). The 10-minute mtime threshold preserved (right liveness signal for CC's jsonl write cadence). Verified on this project (`-Users-cm-THE-AI-osam-fullstack-codebase`): 23 total jsonls across 2 session UUIDs, 3 active in the freshness window → chip correctly renders `Sub 3` (and oscillates `Sub 2 ↔ Sub 3` as jsonls cross the 10-minute boundary). Sentinel `SUBAGENTS-SCAN-V2`. Landed in `fix-statusbar.sh` (not `fix-ruflo.sh` step 11d's V1) because: (i) fix-statusbar.sh already owns the versioned-marker pattern (V1 in MCP-COUNT-PATCH-V1, VECTORS-HNSW-PATCH-V1) and (ii) the V2 block overrides the original V1 block when fix-ruflo runs first then fix-statusbar runs second — the existing step 1d `.bak` + `node --check` guard catches any future write breakage.

13. **Vectors chip undercount by ~21×** (statusline.cjs `getAgentDBStats()` — extends Local Patch 10; fixed via fix-statusbar.sh step 1j with sentinel `VECTORS-HNSW-PATCH-V2`) — V1 (Local Patch 10) counted vectors across 5 embedding-bearing tables (`memory_entries` WHERE embedding NOT NULL, `pattern_embeddings`, `learning_state_embeddings` from `.swarm/memory.db`; `embeddings` and `qe_pattern_embeddings` from `.agentic-qe/memory.db`). The Tier 4 audit enumerated **all** tables with embedding/vector columns across both DBs and found 8 more, three of them with substantial live counts on this project: `aqe.captured_experiences` WHERE embedding NOT NULL = 106; `aqe.vectors` WHERE embedding NOT NULL = 61; `aqe.qe_trajectories` WHERE embedding NOT NULL = 16. Five more (`swarm.patterns`, `aqe.concept_nodes`, `aqe.pattern_versions`, `aqe.hypergraph_nodes`, `aqe.sona_patterns`) sit at 0 today but are part of the same schema and will grow over time. The chip rendered `●9` while reality was `●192` — a 21× undercount that flattened a 9-month timeline of "growing vectors" feedback into a single visible digit. V2 extends V1's `dbVectors` sum with the 8 missed tables, each wrapped in the existing `tableExists()` guard from V1 so future AQE schema bumps fail soft. The HNSW chip's vector-count-keyed speedup ladder (`<1000 → 10x`, `<10000 → 150x`, else `12500x`) now operates on the real total; on this project the chip flipped from `⚡ HNSW 10x` (9 vectors) toward `⚡ HNSW 150x` once enough activity accumulates past the 1k threshold. V1's sentinel `VECTORS-HNSW-PATCH-V1` remains in `statusline.cjs` as a one-shot V1 idempotency guard so older installs that haven't yet re-run `fix-statusbar.sh` since the V2 ship don't double-apply V1.

14. **`💾 MB` chip measured the statusline renderer's own V8 heap** (statusline.cjs `getSystemMetrics()` line 383; fixed via fix-statusbar.sh step 1k with sentinel `MEMORYMB-DBSIZE-V1`) — original implementation was `const memoryMB = Math.floor(process.memoryUsage().heapUsed / 1024 / 1024)`. That measures the renderer process's own V8 heap at the moment Claude Code spawns it — useless. The chip's intent has always been "how much memory is ruflo using"; the implementation reported the renderer's warmth instead. On this project the real on-disk DB footprint is ~3.8MB (`.swarm/memory.db` 200KB + `.agentic-qe/memory.db` 2.1MB + `.swarm/hnsw.index` 1.5MB) but the chip displayed `💾 4MB` because V8 had warmed to that allocation. Replaced with `(safeStat(swarm).size + safeStat(aqe).size + safeStat(hnsw).size) / 1024 / 1024`, falling back to 0 per safeStat's null guard when a file is missing. Icon `💾` and `MB` suffix preserved per the patch spec — the meaning of the chip changed (memory used by ruflo state, not memory used by the renderer), but the visual contract didn't. On this project: chip now reads `💾 3MB` (floor of 3.81), tracking real growth of memory.db + hnsw.index over time. Note the `Size N.MB` chip on the AgentDB line is a separate sum that already reads from real DB files; this fix brings the swarm-line chip into agreement.

15. **`🧠 %` chip derived from a SQLite page-count heuristic** (statusline.cjs `getLearningStats()` step 3, lines 205–248; fixed via fix-statusbar.sh step 1l with sentinel `LEARNING-PATTERNS-V1`) — when neither `.claude-flow/data/patterns.json` nor `.claude-flow/data/auto-memory-store.json` exists (the normal case on a v3 ruflo install), the prior step 3 opened `.swarm/memory.db`, read bytes 28–31 of the SQLite header as a uint32 page count, and reported `pageCount - 2` as the pattern count. The page count is a property of the file (4096-byte pages on disk), not the pattern table (row count). On a 200KB DB that's `(204800/4096) - 2 = 48`; downstream `floor(48/20) * 5 = 5%` math turned it into a chip percentage that had nothing to do with learned patterns. Verified: `SELECT COUNT(*) FROM patterns` on this project returned 0 while the chip displayed `🧠 2%` (because page count had since shrunk to ~10). Now: `_le_count()` helper does `SELECT 1 FROM sqlite_master WHERE name='patterns' LIMIT 1` (tableExists guard) then `SELECT COUNT(*) FROM patterns` against `.swarm/memory.db` and the same against `qe_patterns` in `.agentic-qe/memory.db`, summed. On this project today: `swarm.patterns = 0`, `aqe.qe_patterns = 3` → `realPatterns = 3` → `fromPatterns = floor(3/20) = 0`. `intelligencePct = max(fromPatterns, fromVectors)` (existing logic at line 392); with `fromVectors = floor(192/20) = 9` after Patch 13's V2 vector count, the chip renders `🧠 9%`. The 9% is the honest current state: 0 SONA patterns but 192 vectorised entries indexed through HNSW — both legitimate sources of "intelligence" that will grow over time. The SQLite-header reader still appears in the file in step 3's other paths (the RFE1-magic guard from ruflo#1989 hasn't been touched), but the page-count math itself is gone. **Note**: this patch also retires the `_INSTRUCTIONS.md` follow-up at line 310 ("`getLearningStats` page-count chip — replace with `SELECT COUNT(*) FROM patterns` whenever the section is touched again"). It has now been touched.

16. **AgentDB chip mixed ruflo + AQE state; AQE footer surfaced no vector signal** (statusline.cjs `getAgentDBStats()` + `getAQEStats()` + Line-6 footer + brain-input line 391; fixed via fix-statusbar.sh step 1m with three sentinels `VECTORS-HNSW-PATCH-V3`, `AQE-FOOTER-V2`, `BRAIN-INPUT-TOTAL-V1`) — Tier 4's Patch 13 (`VECTORS-HNSW-PATCH-V2`) **discovered** 8 additional embedding-bearing tables across both DBs and summed them into the AgentDB Vectors chip. That was correct as a discovery effort but wrong as a final routing: the chip's label "AgentDB" + the row's `📊` icon implied "ruflo's AgentDB state", but the number now reflected ruflo + AQE combined. Meanwhile the AQE row showed only `🎓 patterns` + `🧭 traj`, surfacing no vector or DB-size signal of its own — even though AQE was the **dominant** vector producer (195 of 201 vectors on this project came from AQE tables). Tier 5 splits the two domains: **(A) `VECTORS-HNSW-PATCH-V3`** — wholesale rewrite of `getAgentDBStats()` (anchored on the function declaration, not the comment — lesson from prior swarm rounds about AQE clobbering comment anchors) returns three vector fields (`vectorCount` ruflo-only from 4 swarm tables, `aqeVectorCount` AQE-only from 9 aqe tables, `totalVectorCount` = sum) and two size fields (`dbSizeKB` ruflo-only = `.swarm/memory.db` + `.swarm/hnsw.index`; `aqeDbSizeKB` AQE-only = `.agentic-qe/memory.db`); HNSW indicators split similarly (`hasHnsw` ruflo-only based on `vector_indexes` table or `.swarm/hnsw.index` file; `aqeHasHnsw` based on `qe_pattern_embeddings` proxy). All V2-discovered tables relocated to their right domain field — same `tableExists()` guards, same `WHERE embedding IS NOT NULL` predicates. **(B) `AQE-FOOTER-V2`** — `getAQEStats()` now also returns the AQE-domain V3 fields, and the Line-6 render emits `🎓 N patterns │ 🧭 M traj │ 🧬 V vec⚡ │ 💾 X.XMB`. The `🧬` (DNA glyph) is intentionally distinct from the `📊` AgentDB row — visually communicates "different domain". `⚡` appears next to `🧬 vec` when `aqeHasHnsw` is true. `💾 X.XMB` formats via `(kb/1024).toFixed(1)`. **(C) `BRAIN-INPUT-TOTAL-V1`** — single-line swap from `agentdb.vectorCount` to `agentdb.totalVectorCount` in the `fromVectors` calculation at line 391. Without this swap, V3's narrowing would have dropped brain intelligence% from 10% to 0% on a project with 6 ruflo + 195 AQE vectors, which would have been visually regressive AND misrepresented the kit-wide intelligence signal (brain is intentionally a cross-domain summary). Live values on this project after Tier 5: AgentDB `●6⚡` (was `●201⚡` mixed), Size `1.7MB` (was `2.4MB` mixed), AQE footer `🎓 3 │ 🧭 17 │ 🧬 195 vec⚡ │ 💾 2.3MB` (was just `🎓 3 │ 🧭 17`), brain `🧠 10%` (unchanged via totalVectorCount=201). Implementation detail: V3 retains the literal strings `VECTORS-HNSW-PATCH-V1` and `VECTORS-HNSW-PATCH-V2` inside its function comment header so V1 and V2's idempotency-check sentinels short-circuit cleanly on re-run (without this, V1's comment-anchored regex would re-match V3's function on every run and trigger a destructive V1→V2→V3 rewrite cycle). The V3 patcher also self-heals: if the V3 sentinel is present but legacy V1/V2 sentinels are missing in the live file, V3 re-applies once to embed them — covers the upgrade path from a Tier 4 install. Patch 13 entry above describes V2's table discovery; the actual live count there has since been re-routed by V3 — read Patch 13 for the "what tables" story, this entry for the "what domain" story. Patch 14 (`MEMORYMB-DBSIZE-V1`) deliberately stays kit-total (swarm.memory.db + agentic-qe.memory.db + swarm.hnsw.index) — the swarm-row `💾 MB` chip is the kit-wide footprint counterpart to the per-domain Size chips, not a duplicate.

#### Statusline domain semantics

After Tier 5 the statusline chips split cleanly by data-source domain. This table is the canonical reference for "if this chip changes, what changed?":

| Chip | Row | Domain | Source |
|---|---|---|---|
| `🤖 Swarm [N/15]` | Swarm | ruflo | `.claude-flow/swarm/swarm-state.json` agent count |
| `👥 Sub N` | Swarm | Claude Code | `~/.claude/projects/<key>/<session>/subagents/*.jsonl` (mtime<10min) |
| `🪝 N/M` | Swarm | ruflo | `N` = hooks **wired** in `.claude/settings.json` hooks block; `M` = hooks **defined** by ruflo's current template (`ruflo hooks list`). Tier 6.2 clarifies: this is `wired/defined`, NOT `pre-task active vs retired`. Once all defined hooks are wired, the chip stays at `N/N` green. |
| `🔴 CVE N/M` | Swarm | ruflo | `.claude-flow/data/security-snapshot.json` |
| `💾 NMB` | Swarm | **kit-total** | `.swarm/memory.db` + `.agentic-qe/memory.db` + `.swarm/hnsw.index` |
| `🧠 N%` | Swarm | **ruflo** | MoE routing accuracy via `_ra_intelligence()` from `ruflo hooks intelligence stats` (Patch 20.7). The pre-3.10.x kit-total ladder (patches 15 + BRAIN-INPUT-TOTAL-V1) is superseded — overlay wins on aqe-3.10.x |
| `📶 SONA` | Swarm-area | **ruflo** | trajectories/patterns/quality learning ladder (Patch 20.7 `RUFLO-INTEL-V1`) |
| `📊 AgentDB Vectors ●N⚡` | AgentDB | **ruflo only** | 4 swarm tables (Tier 5 VECTORS-HNSW-PATCH-V3) |
| `Size N.NMB` (AgentDB row) | AgentDB | **ruflo only** | `.swarm/memory.db` + `.swarm/hnsw.index` (Tier 5 VECTORS-HNSW-PATCH-V3) |
| `MCP ●N/M` | AgentDB | ruflo + Claude Code allowlist | user-scope `mcpServers` + project-scope `.mcp.json` ∩ `enabledMcpjsonServers` |
| `🎓 N patterns` | AQE footer | **AQE only** | `qe_patterns` table |
| `🧭 N traj` | AQE footer | **AQE only** | `qe_trajectories` table |
| `🧬 N vec⚡` | AQE footer | **AQE only** | 9 aqe tables (Tier 5 AQE-FOOTER-V2); `⚡` if HNSW proxy true |
| `💾 N.NMB` (AQE footer) | AQE footer | **AQE only** | `.agentic-qe/memory.db` (Tier 5 AQE-FOOTER-V2) |

The rule of thumb: domain-specific chips live on domain-specific rows (`📊 AgentDB` row → ruflo; `▊ Agentic QE v3` row → AQE). Cross-domain summary chips live on the universal `🤖 Swarm` row (`💾 MB` kit-footprint, `🧠 %` kit-intelligence). Re-run `bin/ruflo-kit fix-statusbar <target>` to (re-)apply all patches after upgrades.

17. **Tier 6 operational + diagnostic cluster** — five connected gaps that surfaced once the kit was running steady-state long enough to observe its own degradation patterns. **(17.1) Missing agentdb MCP server registration** (fix-ruflo.sh step 5b + 5c) — `.mcp.json` previously listed only `claude-flow` (ruflo) and `agentic-qe`; `agentdb` itself was not registered as its own MCP entry. Without it, Claude Code never exposed the agentdb-direct tool family (`attention`, `reflexion`, `skills`, `causal`, `learning-session`) — those tools are gated on the agentdb MCP server being live, not on the `mcp__claude-flow__*` proxy. Now: jq-driven idempotent merge adds `agentdb@3.0.0-alpha.10` (pinned pre-pivot — see the quarterly-review note below for why this is a deliberate downgrade that restores the 7 controllers removed in alpha.12) with `npm_config_update_notifier: false` + `NODE_ENV: production` and NO `autoStart` (matches agentic-qe's shape — opt-in via `enabledMcpjsonServers`). A separate step 5c jq merge opts agentdb into `.claude/settings.json`'s `enabledMcpjsonServers` allowlist (required for project-scope servers per Patch 9's logic). After this patch, MCP chip renders `●3/4` until Claude Code restart, then `●4/4` once the agentdb server actually starts. jq is required; absence fails loudly with `brew install jq` hint rather than silently corrupting the file. **(17.2) `--force` didn't remove the on-disk hnsw.index sidecar + no post-build invariant** (init-ruflo-aqe-agentdb.sh step 7C) — when `--force` was passed, the sidecar's mere existence tripped the skip-check; the rebuild never fired. Now: `--force && -f $HNSW_INDEX` removes the file (with size info line and dry-run guard) before triggering the lazy build. Plus a post-build invariant check: `vector_indexes.total_vectors` (summed across rows) must equal `COUNT(*) FROM memory_entries WHERE embedding IS NOT NULL`. Drift indicates a build that aborted mid-write, an upstream schema change to `vector_indexes`, or — most common — vectors written via channels other than the standard `memory store` path (which is exactly what triggered this patch: on this project today `total_vectors=0` while `memory_entries(WHERE embedding NOT NULL)=6`, surfaced by the new check). Uses `sqlite3 -readonly` (Patch 10's load-bearing flag). **(17.3) Louder orphan diagnostic** (fix-ruflo.sh step 3, after the orphan-removal loop) — the original loop removed agentdb@<3 orphans cleanly but presented as a single passive `pass` line, leading users to wonder why the same orphan kept reappearing after every npx wipe. Now: when `NPX_SHADOW_COUNT > 0`, two follow-up `warn` lines explain that ruflo's transitive deps include a nested `agentdb@2` which npm re-hoists on every `npx -y ruflo` call; the loop cleans the orphan but does not prevent the re-hoist. Closes a recurring "is this script broken" question. **(17.4) ONNX model cache preserved across npx wipes** (init-ruflo-aqe-agentdb.sh step 1 + upgrade-2026-05-26.sh step 5) — every `~/.npm/_npx` wipe (upgrade script, manual cleanup) discarded the ~25MB Xenova/all-MiniLM-L6-v2 model weights cached inside the npx tree. Next session: re-download from HuggingFace. Now: init exports `TRANSFORMERS_CACHE=${RUFLO_MODEL_CACHE:-$HOME/.cache/ruflo-models}` (per-user persistent cache), and the upgrade script preserves any existing `@xenova/transformers/.cache` subtrees into that location BEFORE the wipe (using `rsync -a --update` when available, falling back to `cp -R`). Both ruflo and AQE honor `TRANSFORMERS_CACHE` automatically. `RUFLO_MODEL_CACHE` env var overrides the path. **(17.5) HNSW backend visibility** (ruflo-health.sh) — `@ruvector/core` ships both a NAPI (native node addon, ~50× faster) and a WASM fallback. NAPI is dlopen'd at runtime; if the platform-arch `.node` binary is missing or fails to load, ruflo silently falls back to WASM — no log, no warning, just a stealth perf cliff. Now: `ruflo-health.sh` runs a filesystem probe (no `require()` — would force the heavy WASM init and skew the health-check cost budget) looking for `rvf-node.${process.platform}-${process.arch}.node` under `~/.npm/_npx`. Detects NAPI (renders `runtime.hnswBackend NAPI (darwin-arm64)` green/dim stable), WASM fallback (yellow `!` warn), or unknown (yellow `!` warn). Captured in the JSON snapshot under `metrics.runtime.hnswBackend`. **Important arch detection nuance**: `uname -m` on a Rosetta-emulated shell on Apple Silicon reports `x86_64`, but `node -e "process.arch"` correctly reports `arm64`. The probe uses `node`'s view because that's the runtime ruflo actually loads under. Without this, users on Apple Silicon running Node via Rosetta would silently see WASM-speed search forever and never know.

18. **Tier 7 — activating ruflo's 7 dormant AgentDB controllers via a pinned-alpha.10 bundle + global-launch switch** (`.mcp.json` claude-flow entry + fix-ruflo.sh Step 3b). `mcp__claude-flow__agentdb_controllers` reported **16/23 active, 7 disabled** (`mutationGuard`, `gnnService`, `attestationLog`, `semanticRouter`, `guardedVectorBackend`, `rvfOptimizer`, `graphAdapter`). **Root cause** (not the standalone `agentdb` MCP server from Patch 17.1 — a different agentdb entirely): ruflo's embedded controller registry at `@claude-flow/memory/dist/controller-registry.js` gates each of those 7 on a bare `await import('agentdb')` exposing a class — `MutationGuard`, `AttestationLog`, `GNNService`, `RVFOptimizer`, `GuardedVectorBackend`, `SemanticRouter`. `@claude-flow/memory` declares `agentdb ^3.0.0-alpha.14`, but **alpha.14 regressed those exports** in the alpha.12 architectural pivot (verified: alpha.14 and the stray bundled 2.0.0-alpha.3.7 both export 0 of them; **alpha.10 exports 6 of the 7 as top-level classes**). So whichever 3.x resolves, the controllers stay dark. **`graphAdapter` (the 7th)** is *not* a top-level class export in alpha.10 — a minimal/uninitialized AgentDB probe shows it absent, which initially looked like a hard 6/7 ceiling — **but on the fully-initialized live server instance the registry reads it via the internal `agentdb.graphAdapter` field, which IS populated at runtime.** Net result verified live: **all 7 recover (23/23 controllers enabled)** after the fix + restart. **The npx trap** (the load-bearing finding): the live MCP launched via `npx -y ruflo@latest mcp start`, and *every* npx-ruflo invocation reconciles its cache tree back to the declared `^alpha.14`, clobbering any alpha.10 we drop into `…/@claude-flow/memory/node_modules/agentdb` — on every Claude Code startup, *before* the registry loads. Patching the npx cache is therefore futile. **Fix**: (a) `.mcp.json`'s `claude-flow` server now launches from the **global** ruflo binary (`command:"ruflo"`, not `npx -y ruflo@latest`) — the global install is never reconciled by npx, so the alpha.10 swap sticks (verified: a real `ruflo mcp start` boot leaves the nested copy at alpha.10; all 6 classes construct). Tradeoff: the live MCP no longer auto-fetches `@latest` on each start — ruflo upgrades now flow through `npm i -g ruflo` + `upgrade-2026-05-26.sh`, which must re-run `fix-ruflo.sh` afterward. (b) **fix-ruflo.sh Step 3b** force-installs `agentdb@3.0.0-alpha.10` into the **global** `@claude-flow/memory/node_modules/agentdb` slot (idempotent; reversible `.prealpha10-bak`; verifies the 6 classes import via the real ESM path before declaring success). It maintains a stable, wipe-proof source at `~/.cache/ruflo-agentdb-3.0.0-alpha.10` (populated once via `npm install --prefix`), and scans **global trees only** (`~/.nvm/**`, `/usr/local`, `/opt/homebrew`) — npx caches are deliberately skipped because they revert. **Activation requires a Claude Code restart** (the running MCP holds the old agentdb in its ESM module cache for the process lifetime); after restart, `agentdb_controllers` reports **23/23 active** — verified live (`active:23, total:23`, up from 16/23). **Quarterly-review caveat**: do not "fix" the declared `^3.0.0-alpha.14` by bumping — alpha.14 is the regression; alpha.10 is deliberately lower. Re-check upstream only when agentdb republishes the 6 controller classes at a version ≥ the current pin.

19. **Tier 7 follow-up — CLAUDE.md CLI-reference normalization** (fix-ruflo.sh Step 5d). `ruflo init` + `aqe init` generate the project `CLAUDE.md` with the PRE-rename package name `@claude-flow/cli@latest` in every CLI example (16 occurrences on this project) and a Setup line that registers the MCP via `npx -y @claude-flow/cli@latest` (wrong package, wrong npx launch model, missing `mcp start`). A swarm audit (2 reviewers) confirmed against live state: the working binary is `ruflo` v3.10.4 (`@claude-flow/cli` is the dead name), and `ruflo --help` lists **37** top-level commands (CLAUDE.md claimed "26"). **Step 5d** idempotently rewrites these the same way Step 5 fixes `.mcp.json`: (a) the Setup line → `claude mcp add claude-flow -- ruflo mcp start` (special-cased FIRST, since the generic `npx -y …`→`ruflo` collapse would otherwise drop the `mcp start` subcommand); (b) all `npx @claude-flow/cli@latest` → `ruflo` (global binary — correct for one-off CLI examples; matches Tier 7's global-launch model); (c) `26 commands` → `37 commands`. `.bak`-guarded with a post-write check that restores the backup if any `@claude-flow/cli@latest` survives. **Verified TRUE by the reality-checker (no fix needed):** `.claude/agents/v3/` exists with the `qe-*` agents; `ruflo doctor --fix` is real; all 8 named `mcp__agentic-qe__*` tools exist; `aqe quality/test/coverage` subcommands work; `.agentic-qe/config.yaml` exists. **NOT auto-fixed (escalated — prose authoring, not mechanical):** (1) the "SendMessage-First Coordination" section (CLAUDE.md ~15-57) and its example at ~28-37 use `subagent_type` values (`researcher`, `system-architect`, `coder`, `tester`, `reviewer`) that the Agent tool **rejects** — only `general-purpose`, `Explore`, `Plan`, and the `qe-*` family are valid — so the example errors if run literally; and the "Wait for X / SendMessage chaining" model empirically makes async background subagents bail without working (fan-out, each agent self-contained reporting to the lead, is what works). (2) CLAUDE.md never states the load-bearing Tier 7 fact that the claude-flow MCP launches from the **global** `ruflo` binary + the separate agentdb MCP server. Both were subsequently **RESOLVED**: the "SendMessage-First" section was rewritten to the fan-out model (valid `subagent_type` values + lead-driven sequencing) and a `## Runtime (operational facts)` Tier-7 note was added — applied directly to CLAUDE.md and made **durable against `ruflo init --force` regeneration** by fix-ruflo.sh **Step 5i** (see Patch 20.1). **[LATER REVERSED — see Patch 28:** the user subsequently adopted the stock **SendMessage-First** section (the role `subagent_type`s became resolvable after the Patch-26 agent backfill), so Step 5i was changed to STOP forcing fan-out — it now only ensures the Runtime note and leaves Agent Comms as-is. The fan-out reliability caveat still stands; revisit if SendMessage-First waits underperform.**]**

**Step 5e (.claude/ content normalization)** extends the same idea to the `ruflo init`/`aqe init`-generated `.claude/` tree. A 3-reviewer swarm found the drift is small + concentrated (skills 97% clean; only 1 of 288 command/agent defs had live errors; v3 agent defs healthy). Step 5e mechanically + idempotently normalizes the confirmed files: `commands/swarm/swarm.md` (`@claude-flow/cli`→`ruflo` CLI refs only), `skills/browser/SKILL.md`, `helpers/pre-commit`, `helpers/post-commit` (CLI refs), and `docs/v3-agents-index.md` (`v3-qe-*`→`qe-*`, names that don't exist as files). **It no longer blanket-remaps `subagent_type` example VALUES** — the earlier `researcher/architect/coder/tester/reviewer → general-purpose/qe-*` rewrite was REMOVED (it was premised on those types being invalid, an artifact of an INCOMPLETE init; the Patch-26 `.claude` backfill restores the agent defs so `researcher`/`coder`/`tester`/`reviewer` resolve to `core/*.md`, and rewriting valid types would be wrong). **The ONE exception kept: `architect` → `system-architect`** — no def declares `name: architect` (the real one is `system-architect` via `architecture/arch-system-design.md`, also the value the stock CLAUDE.md uses), so that single unresolvable stock value IS still normalized. See Patch 26/28. It deliberately SKIPS: (a) `settings.json` config issues — `includeCoAuthoredBy:true` (contradicts the Co-Authored-By policy; verified it did not affect existing commits), dead `permissions.allow` entries (`Bash(npx @claude-flow*)`, malformed `mcp__claude-flow__:*`), and a stale third MCP server `mcpServers.ruflo: npx ruflo@3.5.18 mcp` (duplicates `.mcp.json`'s global claude-flow) — left for a config-hygiene decision; (b) the ~120-file legacy `claude-flow`/`./claude-flow` command catalog + ~286 skill lines — a blind `claude-flow`→`ruflo` rename there produces plausible-but-WRONG subcommands, so it needs human triage (delete dead categories vs leave inert); (c) `helpers/sync-v3-metrics.sh` (its `@claude-flow/cli` is a filesystem-path probe, not a CLI call); (d) AQE hooks invoking `npx agentic-qe` + dead config (`v3-domain-workers.json` unreferenced, `cross-phase-memory.yaml` stub) — needs investigation. `.claude/` is untracked, so the script is the source of truth; re-running re-applies the normalization after any regeneration.

**Step 5f (settings.json hygiene)** jq-fixes three verified config misalignments, idempotently + .bak-guarded: (1) `includeCoAuthoredBy: true` → `false` (the flag contradicted the Co-Authored-By policy; a git-trailer check confirmed existing commits were NOT affected, but the setting was wrong); (2) `permissions.allow` — `Bash(npx @claude-flow*)` → `Bash(npx ruflo*)`, `Bash(npx claude-flow*)` → `Bash(ruflo*)`, and the malformed `mcp__claude-flow__:*` (stray colon) → `mcp__claude-flow__*`; (3) removed `mcpServers.ruflo` (a third claude-flow MCP via `npx ruflo@3.5.18 mcp` — an old npx-pinned duplicate of `.mcp.json`'s global `claude-flow`).

**Investigated, NO action needed (Tier 7 swarm):** (a) the AQE lifecycle hooks in settings.json (`npx agentic-qe hooks <verb>`) WORK — `aqe`==`agentic-qe` (global `agentic-qe@3.10.1`), resolves instantly from a warm npx cache (~0.7s, no network), and every verb incl. `guard`/`post-route` is real; the visible `[CoherenceService]`/`[RvfPatternStore]` SessionStart output is the working hook; the WASM "degraded mode" line is cosmetic. (b) `.claude/hooks/v3-domain-workers.json` + `cross-phase-memory.yaml` are genuinely dead init-scaffold (written-if-absent by init, never read; daemon uses settings.json `claudeFlow.daemon.workers`) — safe to delete but purely optional housekeeping.

**Legacy `claude-flow` command corpus — triaged, NOT auto-fixed:** 163 files reference the old `claude-flow`/`./claude-flow`/`@alpha` CLI. A blind rename would corrupt >40%. Triage classification: **SAFE-RENAME (~30 files)** — `commands/agents/`, `commands/hive-mind/`, `commands/coordination/` (ruflo has matching subcommands); **DEAD (~70 files)** — `commands/sparc/` (32), `agents/sparc/` (4), `commands/github/` (11), `automation/`/`analysis/`/`optimization/`/`monitoring/` (removed features — `ruflo <cmd>` errors → delete, do NOT rename); **NEEDS-MAPPING (~60 files)** — `memory/` (`query`→`retrieve`), `swarm/` (some verbs gone), `hooks/` (`hook`→`hooks` + dead verbs), `workflows/`, top-level `claude-flow-*.md`, v3 skills (per-command transforms + human review).

**Step 5g** safe-renames the SAFE-RENAME set (agents/hive-mind/coordination, prefixed CLI forms only — `@claude-flow/memory` untouched). **Step 5h** handles the NEEDS-MAPPING set with a *verified-verb allowlist*: it rewrites ONLY `(npx[ -y]|./)claude-flow[@alpha] <cmd> <verb>` invocations whose `<verb>` actually exists in `ruflo <cmd> --help` — `memory query→retrieve`, `memory search`, `swarm init`, `workflow execute→run`, and `hook {pre-edit,post-edit,pre-task,post-task,session-end}→hooks <verb>`. **Dead verbs are deliberately left as `claude-flow …`** (an honest legacy marker — fabricating `ruflo memory persist` would be worse): `memory persist/usage`, `swarm spawn` + the `swarm swarm-*` filename-echo artifacts, `workflow create/dev/export/research`, `hook pre-write/check-protected`. The files documenting those dead verbs (`memory-persist.md`, `memory-usage.md`, `swarm-spawn.md`, `swarm-monitor.md`, `workflow-create.md`, `workflow-export.md`, etc.) are effectively DEAD (removed-feature docs) and are DELETE candidates alongside the Step-5g-era DEAD bucket — pending user direction. The `sed -E` prefix match requires `npx`/`./` + a space, so package paths like `@claude-flow/memory` are never matched.

20. **Tier 7 — full `--force` regeneration test + self-heal gaps it exposed.** Running `init-ruflo-aqe-agentdb.sh --force` (which re-runs `ruflo init --force` + `aqe init --auto --upgrade`, regenerating config from scratch) is the kit's end-to-end idempotency test: regeneration reverts files toward upstream defaults, then the script's own Steps 3b/5/5d–5h + fix-statusbar must re-converge them. A swarm audit of one such run found most invariants self-healed (settings.json hygiene, global agentdb alpha.10, CLAUDE.md CLI refs, `.claude/` content, statusline) but TWO regressions were NOT covered — now scripted: **(20.1) CLAUDE.md prose reverted (Step 5i).** `ruflo init --force` regenerates CLAUDE.md, restoring the broken "SendMessage-First" coordination section (invalid `subagent_type` examples + wait-for-peer chaining) and dropping the Tier-7 "Runtime" note. Step 5d only re-fixes CLI refs. New **Step 5i** idempotently rewrites the `## Agent Comms` block to the fan-out model and re-appends the `## Runtime (operational facts)` note (Python multi-line replace, `.bak`-guarded with a structural sanity check that the `## Swarm & Routing` + `## Agentic QE v3` anchors survive). **[REVISED — see Patches 26/28: the "broken SendMessage-First / invalid subagent_type" premise was an artifact of an INCOMPLETE init (missing agent defs). The Patch-26 backfill makes those role types resolve, and the user adopted stock SendMessage-First — so Step 5i NO LONGER rewrites Agent Comms to fan-out; it now ONLY ensures the Runtime note and leaves the Agent Comms section as-is.]** **(20.2) `.mcp.json` duplicate `ruflo` server (Step 5b.1).** `ruflo init --force` re-adds an `mcpServers.ruflo` = `npx -y ruflo@latest mcp start` entry — a redundant, npx-launched duplicate of the canonical `claude-flow` (global) server (the npx path reverts the agentdb pin per Patch 18/19). New **Step 5b.1** jq-removes `mcpServers.ruflo` when both it and `claude-flow` exist. **Activation hardening (init Steps 7B/7C):** (a) **7B** — `ruflo embeddings init` fails with `Invalid model ID format: Xenova/all-MiniLM-L6-v2` (its own validator rejects the `/` in ruflo's default model id); embeddings are actually operational (providers report Ready, 384-d vectors stored). 7B now treats that specific error + a Ready provider as a PASS instead of inflating the failure tally. (b) **7C** — the `vector_indexes.total_vectors` invariant is now ADVISORY: verified live that `embeddings index -a rebuild` reports its real vector count but does NOT update that column on ruflo 3.10.x, so the check false-positives; the HNSW index is built and search works, so 7C emits an `info` note (not a warn/fail) and no longer auto-rebuilds (proven not to reconcile the column).

**A SECOND `--force` run then exposed two more regeneration gaps — now scripted: (20.3) statusline.cjs runtime crash (fix-statusbar Step 1d.2).** `aqe init --upgrade` regenerates `statusline.cjs` with a different internal structure: the `getAQEStats` definition is dropped and the `safeStat`/`getAgentDBStats` helpers the V3 chip-patches assumed are absent — yet `AQE-FOOTER-V2`'s Line-6 render still CALLS `getAQEStats()`. Result: `ReferenceError: getAQEStats is not defined` at runtime, which `node --check` (syntax only) misses and settings.json's statusLine `|| echo` fallback silently swallows (user drops to the degraded v3 line). New **Step 1d.2** is a NON-sentinel-gated runtime smoke: it runs `node statusline.cjs`, and if that fails it (re)writes a SELF-CONTAINED `getAQEStats` using only module-level primitives (`fs`/`path`/`CWD`/`execSync`) with a guarded optional `getAgentDBStats()` call — so it renders regardless of which helpers the regenerated file kept. **(20.4) `.mcp.json` ruflo dup re-added AFTER fix-ruflo (init Step 7.5).** Static analysis showed Step 5b.1 (init Step 5) reports "No duplicate", then a Step-7 activation command (`ruflo swarm init` / `hive-mind init`) RE-registers `mcpServers.ruflo` — so the dedup ran too early. New **init Step 7.5** re-runs the jq dedup AFTER the whole activation table. **Residue — RESOLVED by Patch 20.6 below** (was: the aqe-3.10.x regenerated statusline rendered its own native "V3.x / DDD Domains" theme with zero-valued chips + a stale version label, fix-statusbar's chip patches having drifted from aqe's evolving statusline). Step 1d.2 keeps it RENDERING; Step 1n (20.6) restores full chip-data parity.

**(20.5) Native SONA RL learning restored (fix-ruflo Step 3b `ensure_nested_sona`).** The agentdb LearningSystem logs `[LearningSystem] Sona unavailable, using in-memory trajectories` because its `import('@ruvector/sona')` resolves a **STUB** copy (`package.json` + README, **no `index.js`**) → `ERR_MODULE_NOT_FOUND` swallowed by a bare `catch{}` → falls back to in-memory. Root cause is NOT architecture (the `sona.darwin-arm64.node` binary exists) and NOT the Step-3b agentdb copy — it's that the `@ruvector/sona` reachable from the nested agentdb is incomplete. A COMPLETE copy (with `index.js` + the native `sona.<arch>.node`, incl. darwin-arm64) ships inside `@claude-flow/neural`. `ensure_nested_sona` finds a complete `@ruvector/sona/index.js` under the ruflo tree and copies it into the nested agentdb's own `node_modules/@ruvector/sona`, so the import resolves the complete package → `[SonaTrajectoryService] Using native @ruvector/sona` / `Sona RL trajectory learning enabled (engine: native)`. **Native arm64 — Rosetta was investigated and rejected** (it would emulate the whole working native stack to chase two subsystems; and the SONA failure was a missing JS entry, not an arch mismatch — x64 wouldn't have helped). Runs on BOTH the force path and the already-alpha.10 path. Validated across a 3-loop `--force` swarm: short-circuit (sona already present → untouched) AND cold-reinstall (sona pre-removed → reinstalled native) both converge. **GNN remains unavailable** — `@ruvector/gnn-darwin-arm64` publishes no `.node` binary (a 677-byte stub); it degrades to a JS engine on all Apple Silicon. Upstream packaging gap, not locally fixable; accept until `@ruvector` ships a macOS-arm64 GNN binary (or run ruflo on Linux, where the full native stack exists).

**(20.5.1) Top-level `@ruvector/sona` repair (fix-ruflo `ensure_toplevel_sona`, Tier 7.1).** 20.5 fixed only the agentdb-NESTED `@ruvector/sona` (used by AgentDB's LearningSystem). A later audit found a THIRD copy: the ruflo CLI's `@claude-flow/cli/dist/src/services/ruvector-training.js` does its own `import('@ruvector/sona')`, which hoist-resolves to the **top-level `<ruflo>/node_modules/@ruvector/sona`** — and that copy was *also* a stub (no `index.js`, 0 native `.node`). So `require.resolve('@ruvector/sona')` from the CLI failed, `new SonaEngine()` threw (swallowed), `sonaEngine` stayed `null` and `signalsProcessed:0`. `ensure_toplevel_sona()` copies the complete package (from `@claude-flow/neural`, index.js + 8 native binaries) over the top-level stub; called alongside `ensure_nested_sona` on both the force and already-alpha.10 paths; idempotent (`[[ -f $tgt/index.js ]] && return 0`); reversible (stub backed up to `@ruvector/sona.stub-bak`). Verified live: `require.resolve('@ruvector/sona')` now resolves `index.js` and `import` exposes `SonaEngine` (function). **Scope caveat — this fixes LOAD/resolve only.** Full SONA learning is still blocked upstream: the hooks layer routes project actions into the *decoupled* JS `PersistentSonaCoordinator` (`@claude-flow/memory/dist/persistent-sona.js` — note the **memory** package, not cli) which never calls the real `LoRAAdapter.train()` (`.../ruvector/lora-adapter.js:208`, which DOES correctly update the B matrix); and `neural.js:265-271` constructs a fresh adapter and immediately `saveCheckpoint()`s the untrained init (A random, B=0) → the 9 `lora-checkpoint-*.json` are init snapshots, not training. Loading native SONA is a prerequisite for, not a delivery of, real on-device learning.

**(20.5.2) The `train()` seam — wire `endTrajectory` to the real LoRA trainer (fix-ruflo `wire_sona_train`, Tier 7.2, sentinel `SONA-TRAIN-V1`).** This is the "huge issue": ruflo's `LocalSonaCoordinator.endTrajectory` (`@claude-flow/cli/dist/src/memory/intelligence.js`) computes a real verdict reward (success=1.0/partial=0.5/failure=-0.5) but **dead-ends it into ReasoningBank confidence-scalar nudges** — it never calls `LoRAAdapter.train()`, so the adapter B matrix stays zero-init forever (every `lora-checkpoint-*.json` had B=0). A 5-agent clone-based audit (`vendor/{claude-flow,ruvector,ruflo}`) established the decisive fact: the JS `LoRAAdapter` (the only thing that serializes A/B to disk, at `.swarm/lora-weights.json`, `ruvector/lora-adapter.js:50,317`) and the **WASM SONA `microLoRA` are SEPARATE adapters** — SONA's `serialize_state()` deliberately omits A/B (`vendor/ruvector/crates/sona/src/loops/coordinator.rs`), and the JS export plumbing (`ruvector-wasm-unified/.../learning.ts`) is a stub. So driving the WASM path would train an in-memory matrix that never reaches any artifact; the correct, no-Rust-rebuild, reversible seam is the **JS `LoRAAdapter.train()`**. `wire_sona_train` inserts a guarded block into `endTrajectory` (before it clears `currentTrajectorySteps`): for each step with a dim-matching embedding, `getLoRAAdapter().train(emb, emb, reward)` (reward-modulated Hebbian update — `gradOutput=embedding` for direction, signed `reward` for sign/magnitude; lr 0.001, scaling alpha/rank), then `saveWeights()`. **Validated live**: 6 `recordTrajectory(...,'success')` calls drove `.swarm/lora-weights.json` from `B.sumAbs 0 → 0.054` (all 3072 B entries nonzero, totalUpdates 6); a PoC against the raw `LoRAAdapter.train()` independently confirmed B moves off zero. Idempotent (`SONA-TRAIN-V1` guard), reversible (`intelligence.js.sona-train-bak`), `node --check`-gated (restores backup on parse failure). **Scope caveat — this makes training HAPPEN when ruflo's intelligence path is invoked** (`recordTrajectory`/`endTrajectoryWithVerdict`/`hooks task-completed --train-patterns`). It does NOT make ruflo auto-learn from Claude Code activity on this project, because the live `.claude/settings.json` hooks call `agentic-qe`, not `ruflo` (ruflo's hooks are dormant here). Closing that last gap would require wiring ruflo hooks into Claude Code — a separate, larger decision that would duplicate/displace the already-live AQE recording pipeline. Two secondary upstream defects remain unpatched (lower priority): `neural.js` still saves a fresh untrained adapter from the `neural train` command, and `persistent-sona.ts` pattern extraction uses a 64-dim toy hash embedding instead of MiniLM-384.

**(20.5.3) `neural train` checkpoint now trains before saving (fix-ruflo `train_neural_checkpoint`, Tier 7.3, sentinel `NEURAL-CKPT-V1`).** Closes the first of the two 20.5.2 secondary defects. The `neural train` command (`@claude-flow/cli/dist/src/commands/neural.js`) generates MiniLM embeddings for the training data and trains the WASM microLoRA, but then constructed a **fresh** `LoRAAdapter` and immediately `saveCheckpoint()`d it (neural.js:269-271) — serializing the zero-init, so every `lora-checkpoint-*.json` was untrained (B=0). `saveCheckpoint` falls through to the JS `exportWeights()` JSON (the ruvllm pipeline is a no-op on this install — confirmed by the on-disk `{A,B,scaling,config}` schema), so `train_neural_checkpoint` inserts a guarded loop that runs the already-collected `embeddings` through `adapter.train(emb, emb, 1.0)` before `saveCheckpoint`. Validated: a replicated checkpoint block produced `B.sumAbs 0 → 0.057`. Idempotent (`NEURAL-CKPT-V1`), reversible (`neural.js.neural-ckpt-bak`), `node --check`-gated. Remaining 20.5.2 defect (`persistent-sona.ts` 64-dim toy hash embedding) addressed (conformance only) by Patch 21 SONA-EMBED-384 below.

---

## Tier A — record→distil→infer activation (Phase A; 3/3 validated by independent loop-2 production-validator)

**(21.1) AQE distillation unblocked (`AQE-PROMOTE-V1`).** AQE's pattern-promotion was starved by a `WHERE … AND agent != 'cli-hook'` clause in the `aqe learning extract` candidate query — it excluded the 230+ highest-quality experiences (`cli-hook-post-edit`, quality 0.75, success=1, domain code-intelligence), leaving 490 actionable dream_insights with **0 promoted patterns**. The clause lives in the **bundled CLI chunk** `…/agentic-qe/dist/cli/chunks/learning-GGHGJ6B6.js` (NOT the un-bundled `commands/learning.js`, which the running `aqe` ignores — key debugging lesson). Relaxed to `agent IS NOT NULL /* AQE-PROMOTE-V1 */`; the integrity bars (quality≥0.7, successRate≥0.7) are untouched, so no fake-quality promotion. `aqe learning extract` then minted `qe_patterns 1→2`, `qe_pattern_embeddings 2→3` **at mint time**. ⚠️ The minted row is **not durable**: re-initializing the agentic-qe DB (a reinstall) reverts the live count to 1 — the CODE patch (`AQE-PROMOTE-V1`) survives in the chunk, but you must re-run `aqe learning extract` to re-mint. Idempotent (UNIQUE(name,qe_domain,pattern_type)), reversible (`.aqe-promote-bak`). **Codified in `fix-aqe.sh` (Patch 24)** — it re-applies `AQE-PROMOTE-V1` on the bundled CLI chunk (located dynamically); run `bin/ruflo-kit fix-aqe <target> && aqe learning extract` to restore promotion after a reinstall.

**(21.2) Pre-task RAG retrieval (`AQE-RAG-INJECT-V1`).** AQE recorded 289 embedded experiences but never retrieved them — routing inference was live, pattern/experience inference was write-only. New helper `.claude/helpers/aqe-rag-inject.cjs`: reads the task text, embeds it via AQE's MiniLM leaf module (`dist/learning/real-embeddings.js::computeRealEmbedding` — imported in isolation to avoid the fleet-bootstrap OOM), opens `.agentic-qe/memory.db` read-only, cosine-scores the 384-d Float32 experience blobs, and emits the distinct top-3 (sim≥0.40) as `hookSpecificOutput.additionalContext`. Wired as an **additive 2nd** `PreToolUse ^(Task|Agent)$` hook (`continueOnError`, keeps AQE's `pre-task` routing hook). All console routed to stderr (clean JSON stdout); every path exits 0 → can never block a Task spawn. **Confirmed firing live in-chain** (injected 3 real experiences on a subagent spawn). Reversible (`.claude/settings.json.rag-bak`). Lead-observed live; the helper file is the durable artifact. **Update (Tier 10): the "settings.json wiring needs re-apply logic in a follow-up fixer" clause is RESOLVED** — `fix-aqe.sh` now installs `aqe-rag-inject.cjs` (helper loop :89) and idempotently re-wires the `PreToolUse ^(Task|Agent)$` hook (`ensureHook(... 'aqe-rag-inject' ...)`, fix-aqe.sh:124) on every run. See Patch 30.

**(21.3 / Patch 21) SONA-EMBED-384 (fix-ruflo `ensure_sona_embed_384`).** Closes the 20.5.2 `persistent-sona` defect — **conformance only**. `createHashEmbedding` defaulted to 64-dim vs the stack's MiniLM-384; bumped the default to 384 (+ `rvf-learning-store.js DEFAULT_DIMENSIONS 64→384`). Sync/safe/deterministic one-token change (char-hash distributes over `%dim`). Validated: `createHashEmbedding('test')` → length 384, unit-norm. **Caveat: it is still a char-hash, not semantic** — the real MiniLM swap is async and cascades signature changes up the sync trajectory loop, so it's a deferred separate ticket. Codified in `fix-ruflo.sh` (`ensure_sona_embed_384` + 2 call sites), idempotent (`SONA-EMBED-384`), reversible (`.sona-embed-bak`).

---

## Tier B — dual AQE+ruflo learning + harvest (Phase B)

**(22.0) KEY FINDING — ruflo's CLI hook commands do NOT train.** Before wiring ruflo hooks for live learning, smoke-testing proved the intended path is broken: `ruflo hooks post-task -i <id> -s true` throws `TypeError: Cannot read properties of undefined (reading 'patternsUpdated')`; `ruflo hooks task-completed -i <id> --train-patterns` reports `Patterns Learned 0`; **neither writes `.swarm/lora-weights.json` nor increments `trajectoriesRecorded`** (the CLI→`callMCPTool`→MCP-server path does not reach the in-process `intelligence.js` carrying the Tier-7.2 `SONA-TRAIN-V1` patch — same fragmentation class as the AQE bundle). The ONLY reliable trainer is the direct `getLoRAAdapter().train()` primitive (proven: writes `PROJECT/.swarm/lora-weights.json`, B off zero). So Tier B drives that primitive directly rather than the broken CLI. **(Tier 10 update: this mechanism claim is unchanged — the helpers still call `getLoRAAdapter().train()` directly. What changed is the REWARD fed to it: the subagent trainer now derives a varied reward from the objective outcome oracle instead of a constant — see Patch 22.5 and Tier 10 / Patch 29.)**

**(22.1) Dual hook — AQE + ruflo (`RUFLO-TRAIN-V1`).** New helper `.claude/helpers/ruflo-train.cjs`: embeds the edited file/task via AQE's MiniLM leaf module, calls `getLoRAAdapter().train(emb, emb, reward)` + `saveWeights()` → `.swarm/lora-weights.json`. Wired as an **additive 2nd** hook on `PostToolUse ^(Write|Edit|MultiEdit)$` (keeps the AQE `post-edit` hook; both `continueOnError`). All console→stderr, always exits 0 (never blocks an edit). **Confirmed firing live in-chain** (this session's `Write`/`Edit` tool calls incremented `lora-weights.json` totalUpdates). Reversible (`.claude/settings.json.dualhook-bak`). This is the working substitute for the broken `ruflo hooks task-completed` train path.

**(22.2) AQE→ruflo+AgentDB harvester (`tools/aqe-harvest.cjs`, AQE-HARVEST-V1).** *(Snapshot SUPERSEDED by 22.4 — `agentdb.db` is now LIVE-FED via a SessionEnd hook; the `236 episodes` / `previously-EMPTY` / `B.sumAbs 0→1.27, totalUpdates→239` figures below are the ORIGINAL one-shot seed, NOT current state. They grow each session — last checked ~302 episodes / B.sumAbs ~1.8 / 385 updates.)* Batch-replays AQE's 236 high-quality experiences (success=1, quality≥0.7, 384-d embedding) into the dormant ruflo substrate WITHOUT a 2nd competing live hook. **Sink A (ruflo LoRA):** `getLoRAAdapter().train(emb, emb, quality)` → `.swarm/lora-weights.json` **B.sumAbs 0→1.27, totalUpdates→239**. **Sink B (AgentDB):** node-import `new AgentDB({dbPath:./agentdb.db}).initialize()` then `reflexion.storeEpisode` + `skills.createSkill` → **236 episodes + 1 skill** in the previously-EMPTY `agentdb.db` (skills deduped by domain). **Causal edges SKIPPED** — the data has no real cause→effect pairs and no failure→retry sequences (Integrity Rule: no fabricated relations/episodes). Source DB read-only; idempotent via `.swarm/harvest-state.json` ledger (re-run → `nothing fresh`, no double-count). Backup `agentdb.db.harvest-bak`. Tracked, runnable: `bin/ruflo-kit harvest <target>` (implementation `tools/aqe-harvest.cjs`).

**(22.3) Config aligns.** `.claude/settings.json enabledMcpjsonServers` += `claude-flow` → `_ra_mcp` now yields **MCP 3/3** (was 2/3; chip self-heals after the ≤10s statusline cache TTL). Doc notes: `.mcp.json` uses `aqe-mcp` (not the older documented `npx … agentic-qe`); the `🪝 N/M` chip reflects ruflo *template* hook coverage, not the live (AQE+ruflo dual) wiring; **alpha.10 pin retirement-trigger** — upstream `@claude-flow/memory` now declares `agentdb ^3.0.0-alpha.14`, so re-test `agentdb_controllers` 23/23 on each upstream bump and retire Step 3b once an alpha.15+ restores the controller classes.

**(22.4) `agentdb.db` is now LIVE-FED, not a frozen snapshot (`AGENTDB-SPLIT-V1` + `fix-aqe.sh` SessionEnd wiring).** 22.2 left `agentdb.db` as a one-shot manual harvest (it froze at the last manual run while AQE kept writing to `.agentic-qe/memory.db`). Now the harvester is wired as an additive **`SessionEnd`** hook in `.claude/settings.json` (`sh -c 'cd "$CLAUDE_PROJECT_DIR"; node tools/aqe-harvest.cjs'`, `continueOnError`, 60s), codified idempotently in `fix-aqe.sh` via `ensureHook(s.hooks.SessionEnd, '', HARVEST, 'aqe-harvest', 60000)` (dedups across groups; merges into the existing hook-handler group on a fresh install). So every session end batch-replays the fresh AQE experiences (idempotent via the `.swarm/harvest-state.json` ledger — `lastRowid`/`ids` advance; `nothing fresh` when caught up). **Read-back fix:** the harvester's better-sqlite3 backend left `agentdb.db` in WAL mode, after which the statusline's `sqlite3 -readonly` failed with `CANTOPEN(14)` (can't create the `-shm` to read the WAL) → the chip flashed `●0`; `aqe-harvest.cjs` now runs `PRAGMA wal_checkpoint(TRUNCATE)` after `adb.close()` so `-wal` is flushed/removed and read-only reads always succeed. **Chip repoint (`AGENTDB-SPLIT-V1`, fix-statusbar Step 1n (g)):** the `📊 AgentDB` chip used to read `.swarm/memory.db` and undercount; it now reads the real `agentdb.db` (`_ra_agentdb` sums `episode_/pattern_/learning_state_/skill_/note_/exp_node_embeddings`), and a new **`🗃️ Swarm DB`** chip (`_ra_swarmdb`) carries the `.swarm/memory.db` store. Both gate the `⚡` HNSW glyph on `SUM(total_vectors)>0` (or a real `hnsw.index`) — empty `vector_indexes` rows (`total_vectors=0`) no longer fake `⚡` on either chip or the Line-1 perf badge.

---

**(22.5) LoRA training on Task SUBAGENT completion (`.claude/helpers/ruflo-train-subagent.cjs`, `RUFLO-TRAIN-SUBAGENT-V1`).** Closes a learning gap found by the Claude-Code-coordination audit: `ruflo-train.cjs` (22.1) only fires on `PostToolUse` Edit/Write, so all Task **subagent** work (research/analysis/audits — e.g. every fan-out agent) never fed the SONA LoRA. New helper wired on the **`SubagentStop`** hook (additive, beside the existing `hook-handler post-task`): reads the SubagentStop stdin payload, extracts the subagent output from `transcript_path` (the JSONL — task = first user msg + ALL assistant text, conclusion-weighted via `slice(-3500)` since MiniLM truncates ~256 tokens; field fallbacks `result`/`output`/`response`), embeds via AQE MiniLM (384-d), and `getLoRAAdapter().train()+saveWeights()` → `.swarm/lora-weights.json`. **Verified live:** a real SubagentStop moved `B.sumAbs` (linchpin: real payload DOES carry `transcript_path`). **Loop-2 hardening (2 blockers fixed):** (1) a **mkdir-atomic lock** (`.swarm/lora-train.lock`, 120s stale-steal) serializes trainers — concurrent SubagentStops (fan-out) would otherwise interleave ruflo's non-atomic `saveWeights` (`lora-adapter.js` bare `writeFileSync`, upstream) and corrupt the 177KB weights JSON; if the lock is held this run SKIPS (a lost sample beats a corrupt adapter) — verified: 4 parallel trains leave valid JSON; (2) a **min-length gate** (skip <40 chars) drops trivial "done." outputs so the LoRA isn't trained on noise — verified: terse skips, substantive (rich body, terse last turn) trains. ~~Fixed reward 0.8~~ Always exits 0 (never blocks the subagent lifecycle). **REWARD UPDATE (Tier 10 / Patch 29):** the subagent trainer no longer uses a constant 0.8 — `ruflo-train-subagent.cjs:86-88` now derives a **varied** reward via `deriveOutcome(transcriptRaw || body).reward` (the objective outcome oracle, `DERIVE-OUTCOME-V1`), with an explicit `argv[2]` numeric override preserved for tests/replay. A constant reward was the self-improving blocker (every RL estimator converges to a fixed point); the objective oracle restores the reward variance the loop needs. NOTE: only `ruflo-train.cjs` (the `PostToolUse` Edit/Write trainer, :26) still uses the literal `0.8` default — that path was not migrated. Tracked source `assets/claude-helpers/ruflo-train-subagent.cjs`; codified in `fix-aqe.sh` (helper install loop + `ensureHook(s.hooks.SubagentStop, '', TRAINSUB, 'ruflo-train-subagent', 30000)`), idempotent + reversible (`.fixaqe-bak`).

---

## Tier C — ruflo active-brain opt-in + native Rust rebuild (Phase C)

**(23.1) ruflo active-brain opt-in.** **autopilot ✓ ENABLED** (`ruflo autopilot enable` → 50 iterations / 240 min; disable via `ruflo autopilot disable` if it over-continues a session). **guidance ✓ compiled** (`ruflo guidance compile` → CLAUDE.md into 24 shards; `guidance retrieve -t "<task>"` returns intent + relevant shards). **memory-bridge** (`auto-memory-hook.mjs import`) runs but is a **no-op** ("Memory package not available") — documented honestly, not wired/claimed.

**(23.2) Native SONA LoRA export — Rust rebuild (the headline; resolves the audit's hardest blocker).** The audit established that real SONA A/B persistence was UNREACHABLE from the shipped binding (`save_state` serializes only patterns+EWC; `export_lora_state` was unbound). Added a `#[napi] export_lora_state()` to **`crates/sona/src/napi_simple.rs`** (the module the `napi` feature actually compiles — `napi.rs` is dead code, lib.rs:67) that emits the trained micro-LoRA as JSON `{A,B,rank,inputDim,outputDim}` via `serde_json::json!` (no struct changes; `serde-support` is a default feature). Built the arm64 `.node` from `vendor/ruvector` and swapped it into the global `@ruvector/sona` (all 3 copies; **both `sona.darwin-universal.node` AND `sona.darwin-arm64.node`** — the loader prefers *universal* first, so arm64-only swap is shadowed; `.prebuilt-bak` backups). **Verified:** global `new SonaEngine(384).exportLoraState()` returns real `{A,B,rank,inputDim,outputDim}` (A/B len 768 @ rank 2 after a trajectory), existing methods intact. **Build recipe** (reproducible): `npm i -g @napi-rs/cli`; then `RUSTFLAGS="-C link-arg=-undefined -C link-arg=dynamic_lookup" napi build --platform --release -p ruvector-sona --manifest-path crates/sona/Cargo.toml --features napi` → `target/aarch64-apple-darwin/release/libruvector_sona.dylib` → copy as `sona.darwin-{universal,arm64}.node`. **Debug lessons:** napi 3.x dropped `--cargo-cwd` (use `--manifest-path`); macOS cdylib needs the `dynamic_lookup` link flag (the workspace `.cargo/config` wasn't picked up through napi's cwd); napi resolved the wrong workspace package for the bindgen/copy step, so the `.node` is the dylib renamed by hand.

**(23.3) Native GNN arm64 — Rust rebuild (closes the long-standing "GNN unavailable on Apple Silicon" gap).** `@ruvector/gnn-darwin-arm64` shipped as a 677-byte stub (no `.node`), so GNN ran on a JS fallback. Built `ruvector-gnn-node` (cdylib, napi always-on) with the same link flag: `RUSTFLAGS="-C link-arg=-undefined -C link-arg=dynamic_lookup" cargo build --release -p ruvector-gnn-node --target aarch64-apple-darwin` → `libruvector_gnn_node.dylib`, placed as `ruvector-gnn.darwin-arm64.node` in `@ruvector/gnn-darwin-arm64/`. **Verified:** `@ruvector/gnn` now loads the NATIVE binding (`RuvectorLayer`, `TensorCompress`, `NativeRuvectorLayer`, `differentiableSearch`, …) — no JS fallback. This retires the prior Patch 20.5 "GNN remains unavailable, upstream packaging gap" note for darwin-arm64.

**Reproducibility (both native builds) — CODIFIED (`NATIVE-BUILDS-V1`).** The compiled `.node` artifacts live in the GLOBAL ruflo install (lost on `npm i -g ruflo`), so they are committed to **`assets/builds/`** (`sona.darwin-arm64.node`, `ruvector-gnn.darwin-arm64.node`, + the `sona-export_lora_state.napi.patch` source diff) and reinstalled by **`install_native_builds()`** (fix-ruflo.sh, called at both `force_nested_agentdb` paths) into the global `@ruvector/{sona,gnn-darwin-arm64}` on every `fix-ruflo.sh` run — **darwin-arm64-gated, idempotent (`cmp` skips identical), reversible (`.prebuilt-bak` for both the SONA and GNN binaries)**. So the native rebuild survives a ruflo reinstall. The vendor source (with the `napi_simple.rs` edit, captured in the `.napi.patch`) + the build recipes above are the path to regenerate the binaries (e.g. for a new arch or after an upstream version bump). The JS `LoRAAdapter` learning path (Tier B) is unaffected and remains the primary durable learner; the native `exportLoraState` is now AVAILABLE for a future integration that writes real Rust-engine A/B into checkpoints (JS-side wiring is the remaining integration step).

**(24) `fix-aqe.sh` — codifies the AQE-side hardening (companion to `fix-ruflo.sh`).** Closes the "live-applied but not codified" gaps from Patches 21-22: **(1)** re-applies `AQE-PROMOTE-V1` by locating the agentic-qe `learning extract` candidate-query chunk dynamically (`grep -rl "GROUP BY domain HAVING COUNT" dist/cli/chunks/*.js`, version-hash-agnostic, `.js$`-only so the `.aqe-promote-bak` is excluded) and relaxing `agent != 'cli-hook'` → `agent IS NOT NULL /* AQE-PROMOTE-V1 */` (quality/successRate integrity bars untouched; `.aqe-promote-bak`, node-check-gated). **(2)** installs the `.claude` helpers from tracked sources `assets/claude-helpers/` (cmp-skip, `.fixaqe-bak`, each `node --check`-gated) and wires `.claude/settings.json` via an idempotent node modifier. **As of Tier 10 the install loop covers SIX helpers** (fix-aqe.sh:89, ORDER matters — `_derive-outcome.cjs` is listed FIRST because both `aqe-post-route.cjs` and `ruflo-train-subagent.cjs` `require('./_derive-outcome.cjs')` relative to `__dirname`, so the oracle must land in `.claude/helpers/` alongside its consumers): `_derive-outcome.cjs`, `ruflo-train.cjs`, `ruflo-train-subagent.cjs`, `aqe-rag-inject.cjs`, `aqe-post-route.cjs`, `ruflo-route-capture.cjs`. The wiring step (`ensureHook`, dedups across duplicate matcher groups) now covers **FIVE+ hook events**: `PreToolUse ^(Task|Agent)$ += aqe-rag-inject` (AQE-RAG-INJECT-V1, :124); `PostToolUse ^(Write|Edit|MultiEdit)$ += ruflo-train` (RUFLO-TRAIN-V1 dual hook, :126); `SessionEnd += aqe-harvest` (AGENTDB-SPLIT-V1 live-feed, :132); `SubagentStop += ruflo-train-subagent` (:139); `Stop` — **strips the legacy hardcoded `post-route --success true` group** (:147-148) then wires `aqe-post-route` (:149, the objective-oracle Stop wrapper); `UserPromptSubmit += ruflo-route-capture` (RUFLO-ROUTE-CAPTURE-V1, :157); plus `enabledMcpjsonServers += claude-flow` (:159). Idempotent + reversible; matches fix-ruflo.sh conventions (bash 3.2-safe — `while read` arrays, not `mapfile`). Run: `bin/ruflo-kit fix-aqe <target> [--dry-run]`. **Caveat:** the AQE chunk is a *bundled* file in the global install (lost on agentic-qe reinstall) — `fix-aqe.sh` re-applies it; the `.claude` helper sources are now tracked in `assets/claude-helpers/`. See Tier 10 (Patches 29-31) for the objective-reward + Router-B-closure helpers this loop installs.

## Tier 8 — real subscription-backed agent spawn

**(25) `ruflo agent spawn --task` now EXECUTES via the Claude Code subscription (`fix-ruflo.sh` `wire_real_spawn`, `RUFLO-REAL-SPAWN-V1`).** A 3-agent fan-out audit established that **every** ruflo spawn path was either bookkeeping or API-key-billed: `agent spawn` / `agent_spawn` write an IDLE store row and dispatch nothing (`@claude-flow/cli/dist/src/commands/agent.js:135` → `mcp-tools/agent-tools.js:213`, status hardcoded `'idle'`); `agent_execute` + `managed_agent_*` + WASM agents DO run but via a raw `fetch('https://api.anthropic.com/v1/messages')` with `x-api-key` (pay-per-token — and broken here, no key set). The only subscription-backed runner is the local `claude` CLI (used internally by `headless-worker-executor.js:890` and `hive-mind spawn --claude`), but it was unreachable from `agent spawn`. **Fix:** patch the spawn action so that WHEN `--task` is set it spawns `claude --print` (subscription auth, NO `ANTHROPIC_API_KEY`) — prompt piped via stdin (no shell tokenization), output printed, and `.claude-flow/agents/store.json` updated to real `status`/`taskCount`/`lastResult`/`lastActiveAt` (so `agent list` is no longer stuck idle). Bare `agent spawn` (no `--task`) is UNCHANGED. **Loop-2 hardening (3 fixes after a devil's-advocate pass):** (1) clear ALL parent Claude Code session markers — `CLAUDE_SESSION_ID/PARENT_SESSION_ID` + `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT/SSE_PORT/SESSION_ID/EXECPATH` (CLAUDE_ENTRYPOINT=worker alone leaks the parent session when spawned from inside CC); (2) do NOT auto-attach `--mcp-config` (booting agentdb-via-npx + aqe GNN/SONA on every spawn is a hang/cost bomb — pass it yourself if a task needs ruflo tools); (3) add `--dangerously-skip-permissions` (with a transparent `[WARN]`) so real tool tasks don't deadlock on a TTY-less permission prompt — matches the kit's hive-mind/headless-worker subscription spawns. **Verified live (ANTHROPIC_API_KEY unset):** no-tool task → real `PONG`/`VERIFYOK`; tool task → agent wrote `/tmp/spawn-tool-test.txt`=`TOOLOK` in 35s. Codified in `fix-ruflo.sh` (`wire_real_spawn`, anchor `updateSwarmActivityMetrics(1);`, called at BOTH `force_nested_agentdb` sites), idempotent (`RUFLO-REAL-SPAWN-V1` — the sentinel lands in `@claude-flow/cli/dist/src/commands/agent.js:171`, NOT in `hooks-tools.js`), reversible (`.realspawn-bak`), `node --check`-gated, survives `npm i -g ruflo`. **Known minor:** the spawn table prints `Status: registered` then a separate `Task completed` line (registration→execution is two honest phases); `completed` reflects `claude` exit-0, not semantic task success. **Complement (driving ruflo FROM Claude Code):** real coordination = `swarm_init` + `memory_*`/`hooks_route` (substrate) + the native **Task tool with `.claude/agents/v3/qe-*` subagents** (the only subscription executor) + auto-capture hooks; `agent_spawn`/`task_orchestrate`/`hive-mind_spawn`/`coordination_orchestrate` are bookkeeping; no MCP tool bridges a registered row → Task execution (open gap).

---

## Statusline patches (Tier 5/6 — listed here for continuity; predate Tier 8)

**(20.6) Statusline chip-data realigned to aqe-3.10.x (fix-statusbar Step 1n, `AQE310-REALIGN-V1`).** Closes the deferred residue from 20.3: the aqe-3.10.x statusline.cjs RENDERS the full ruflo+AQE chip set (header version, DDD, Swarm/Hooks/CVE/Memory/Intelligence, Architecture, 📊 AgentDB Vectors/Size/MCP, ▊ Agentic QE v3 footer) — but its chips showed zeros/undefined because **the data source was wrong**, not the render. `getStatuslineData()` called the DEAD `npx @claude-flow/cli@latest hooks statusline --json` → fell back to zeros; and the correct `ruflo hooks statusline --json` omits the `agentdb`/`integration`/`hooks` blocks. Step 1n realigns the DATA (patches stable function names, not fragile render anchors): **(a)** repoints the source to global `ruflo hooks statusline` (restores DDD 2/5, ddd%, patterns, swarm active+coordinating, model, security, memory, intelligence); **(b)** fixes a render bug where the hooks chip used raw `hooks.enabled`/`.total` (→ "undefined/undefined" when the CLI omits hooks) instead of the defaulted `hooksEnabled`/`hooksTotal`; **(c)** makes `getPkgVersion` report the real ruflo version (was a hardcoded `'3.6'` default, since its `pkgPaths` don't include the global install); **(d)** overlays the omitted blocks via self-contained helpers (`_ra_agentdb` = real ruflo `.swarm` vector count/size/HNSW; `_ra_mcp` = `.mcp.json` ∩ settings `enabledMcpjsonServers` count; `_ra_hooks` = settings hook-event count) and enriches `getAQEStats` with AQE-domain `🧬 vec` + `💾 MB` via `_ra_aqevec` (`.agentic-qe` embedding tables). Result: full live parity — `▊ RuFlo V3.10.4 … 📊 AgentDB Vectors ●N⚡ │ Size │ MCP ●2/3 … ▊ Agentic QE v3 🎓 N │ 🧭 N traj │ 🧬 N vec⚡ │ 💾 N.NMB`. Sentinel-gated + idempotent + validated to survive `--force` (aqe regenerates statusline → Step 1d.2 backstop reinstalls getAQEStats, then Step 1n re-realigns, in the same run). The MCP chip reads `●2/3` because `claude-flow` loads via the project-scope default rather than `enabledMcpjsonServers` — counts as total-but-not-enabled; cosmetic, matches Patch 9's consent math.

**(20.7) `🧠` chip rewired to a REAL ruflo metric + new `📶 SONA` learning-ladder chip (fix-statusbar Step 1n (f), `RUFLO-INTEL-V1`). Ruflo-only — AQE is explicitly out of scope for this chip.** Root cause is an **upstream ruflo gap**, confirmed by a 3-agent fan-out audit (source + live-telemetry + persistent-store): the `🧠` chip is `system.intelligencePct` from `ruflo hooks statusline --json`, which is *designed* to read a real `intelligence.score` but never gets one, via THREE compounding defects: **(1) PATH MISMATCH** — `ruflo init` writes `learning.json` to `.claude-flow/`**`metrics/`**`learning.json` (`@claude-flow/cli/dist/src/init/executor.js:474-490`), but the reader (`commands/hooks.js:3445-3449`) looks only at `.claude-flow/learning.json`, `.claude/.claude-flow/learning.json`, `.swarm/learning.json` — one directory apart, they never meet; **(2) SCHEMA MISMATCH** — the init template writes only `routing`/`patterns`/`sessions`, **no `intelligence.score`** key (which the reader requires at `hooks.js:3454`); **(3) NO RUNTIME PRODUCER** — no worker/hook/command anywhere writes `intelligence.score` (the learning worker `@claude-flow/hooks/dist/workers/index.js:960-998` only *reads* it). So tier-1 never fires and it falls through to a **file-size proxy**: `intelligencePct = floor(learning.patterns/10)` where `learning.patterns = floor(.swarm/memory.db_mainfileKB / 2)` (`getLearningStats` ~`hooks.js:3332-3354`) ⇒ effectively `.swarm/memory.db_KB / 20` = **9** on this project. That metric is meaningless AND unstable — a routine SQLite WAL checkpoint balloons memory.db and would snap `🧠` from 9% → 100% with zero actual learning change. **The real signal exists and is just unwired:** `ruflo hooks intelligence stats` exposes live **MoE Routing Accuracy** (82%), Avg Quality (75%), Trajectories (495), Patterns Learned, backed by `.claude-flow/neural/stats.json`. **Fix — Step (f) overlays it** (after the realignment + WAL self-heal, sentinel `RUFLO-INTEL-V1`, idempotent, needs the `AQE310-REALIGN-V1` base): a `_ra_intelligence()` helper parses `ruflo hooks intelligence stats` (label-`indexOf` picker — **no regex backslashes**, since the box-table has no `--json`) for Routing Accuracy / Trajectories / Patterns Learned / Avg Quality, with a `.claude-flow/neural/stats.json` count fallback (`trajectoriesRecorded`/`patternsLearned`) when the CLI is unreachable. The overlay sets `data.system.intelligencePct = ri.pct` **only when `ri.pct > 0`** (never regresses the existing value) and attaches `data.system.ruflo` for the new chip. A new render block (injected before `// Line 3: Architecture`) emits **`📶 SONA [●●●○○] N traj │ M patterns │ Q X%`** — a trajectories-driven 5-tier ladder (thresholds `[50,150,350,700,1500]`), color-graded by tier. Live result: `🧠 82%` (real routing accuracy, green) + `📶 SONA [●●●○○] 495 traj │ 7 patterns │ Q 75%`. **Note:** the in-memory routing telemetry ("65% confidence / 164 requests / qWeight") is NOT persisted to any file (computed live by the MCP daemon), and `.claude-flow/data/health-history.jsonl` is **stale** (frozen ~01:32) — so the live `hooks intelligence stats` call is the trustworthy source, not the snapshot file. The extra CLI call is covered by the statusline cache (one exec per cache window; 5s timeout → file fallback). Perf cost accepted.

**(20.8) `🧠` chip rewired again to a TRAINED-LoRA score + SONA `Δ` chip (fix-statusbar Step 1n (f), `RUFLO-INTEL-V2` — supersedes 20.7's V1).** 20.7's V1 surfaced live MoE *Routing Accuracy* (0.82) / *Avg Quality* (0.75) — but those are **fixed upstream constants**, not a moving learning signal. V2 rewires `_ra_intelligence()` so `🧠` is a real ruflo-only learning score = trained micro-LoRA magnitude (`sum|B|` of `.swarm/lora-weights.json`, 0 until real training lands, grows with on-device learning) + real trajectory/pattern counts; the `📶 SONA` chip now shows **`Δ <sum|B|> LoRA`** instead of the hardcoded `Q 75%`; and the `🏗️ DDD Domains` proxy is relabeled **`🏗️ Learning`** / `DDD`→`Learn` (it was a pattern-count proxy, never real DDD). `getPkgVersion` also now live-detects the GLOBAL ruflo `package.json` first. Idempotent (replace-or-insert on `_ra_intelligence`, sentinel `RUFLO-INTEL-V2`, needs the `AQE310-REALIGN-V1` base). Live: `🧠 72-78%` (moves with training) + `📶 SONA [●●●○○] 495 traj │ 7 patterns │ Δ 1.8 LoRA`.

All fixes are byte-minimal and preserve the original control flow. If the upstream gist publishes the same fixes, the local versions can be replaced wholesale; until then, do **not** overwrite these files from the gist without re-applying the patches.

---

## Tier 9 — full template backfill + warning hygiene + SendMessage-First (this session)

**(26) Non-destructive `.claude` template backfill on init (`init-ruflo-aqe-agentdb.sh` Step 4.5).** An AQE-first project ends up with the qe-* agents + only a thin ruflo subset (observed: 79/89 ruflo agents, 8/23 categories) because `ruflo init` and `aqe init` each copy curated/partial sets and ordering leaves gaps. New **Step 4.5** runs after both inits and NON-DESTRUCTIVELY backfills every agent/command/skill present in the global ruflo CLI template (`…/ruflo/node_modules/@claude-flow/cli/.claude`) or the agentic-qe template but MISSING from `.claude/` — only ADDS files, never overwrites (qe-* and customizations preserved). Idempotent; `--dry-run`/`--reactivate`-aware. Applied live: **79→177 agents, 153→176 commands, 412→426 skills** (8→25 agent categories). A Step-9 verification check (`.claude/agents (ruflo full set backfilled)`) was added. NOTE: ruflo PLUGINS are not part of init's `.claude` (the CLI template ships none — install via `ruflo plugin install <name>`); HOOKS live in `.claude/settings.json` (wired by fix-aqe.sh, step 6.5) — there is no `.claude/hooks` template.

**(27) fix-ruflo.sh warning hygiene (3 false-positives silenced → 0-warning run).** Step 11c/11d warned `getSwarmStatus not found` / `estimated_agents not found` because those are LEGACY patches for the pre-realignment statusline; the aqe-3.10.x realigned variant sources swarm from `ruflo hooks statusline --json` and subAgents from `system.subAgents`, so the legacy anchors are intentionally absent — they now `pass` with an explanatory note when `AQE310-REALIGN-V1` is present (only warn for a genuinely old statusline). The `.mcp.json` check warned `may need manual review` because it required `"autoStart": true`, but modern Claude Code `.mcp.json` omits `autoStart` entirely (servers start on demand / via `enabledMcpjsonServers`) — the OK verdict no longer gates on it. `fix-ruflo.sh` now runs **0 warnings** ("Everything looks good!").

**(28) CLAUDE.md adopts stock "SendMessage-First Coordination" — supersedes Patch 19's fan-out reversal (commit 16cd38c).** Patch 19 had rewritten the stock "Agent Comms (SendMessage-First)" section to a custom **Fan-Out** model (citing invalid subagent_types + unreliable wait-chaining) and made it durable via Step 5i. This session the user opted to adopt the stock **SendMessage-First** section instead (its `researcher`/`coder`/`tester` role types are now resolvable after the Patch-26 backfill). CLAUDE.md was refreshed by generating a fresh ruflo+aqe init in an isolated temp dir (no project config touched) and swapping ONLY the Agent Comms section — everything else (ruflo CLI forms, 37-command count, global mcp launch, Runtime facts, AQE v3) preserved, since the rest of fresh stock was a regression (npx `@claude-flow/cli@latest`, "26 commands"). **fix-ruflo.sh Step 5i was updated** to match: it no longer rewrites Agent Comms to fan-out (which would silently revert the user's choice on the next run) — it now ONLY ensures the Tier-7 `## Runtime (operational facts)` note is present and leaves Agent Comms as-is. Caveat: SendMessage-First's "Wait for X" inter-agent chaining is the pattern Patch 19 found unreliable for async background subagents — fan-out remains the empirically-proven approach; revisit if waits underperform.

### Follow-ups (open, non-blocking)

These are gaps identified during the Tier 1 / Tier 1.2 work but consciously deferred:

- **Anchor robustness against vanilla-AQE clobber** (fix-statusbar.sh) — the four patch sub-steps (1c, 1g, 1h, plus the AQE footer) anchor on surrounding comment strings (`// Integration status`, `// AgentDB stats`, etc.) that exist in the ruflo template but not in the AQE-bundled vanilla template. If `statusline.cjs` is overwritten by the AQE vanilla file (rare in the documented bootstrap order, where `fix-ruflo.sh` step 11 ruflo-shapes the file before `fix-statusbar.sh` runs), the patches warn loudly and bypass cleanly — but they don't re-apply. Fix: switch the anchors to match the function declarations directly (`function getIntegrationStatus()`, `function getAgentDBStats()`) instead of the surrounding comments.
- **HNSW speedup ladder is heuristic** — the `10x / 150x / 12500x` ladder in step 1h is keyed off `vectorCount`, not a measured speedup. The SOTA screenshot's `150x` value likely comes from a real `embeddings_status.hnsw.speedup_factor` field once that's exposed by ruflo. When it is, swap the ladder for the live value.
- ~~**Statusline `getAgentdbStats()` `getLearningStats` page-count chip**~~ — **Resolved by Local Patch 15.** `getLearningStats()` step 3 now uses real `SELECT COUNT(*) FROM patterns` + `SELECT COUNT(*) FROM qe_patterns` instead of the page-count heuristic.
- **`pgrep` liveness cross-check for MCP chip** — Patch 9 corrected the arithmetic but doesn't verify the configured server is actually responding. Add `pgrep -f "ruflo mcp start"` so the chip turns red when the config says enabled but no process is alive.
- **VECTORS-HNSW-PATCH-V2 table list ages with AQE schema** — Local Patch 13's V2 sum enumerates 13 specific embedding-bearing tables across `.swarm/memory.db` and `.agentic-qe/memory.db`. Each is wrapped in `tableExists()` so a removed table fails soft, but a newly **added** embedding-bearing table in a future AQE schema would be silently uncounted. Action: when AQE schema bumps (track via `aqe --version` deltas), re-run the audit query `SELECT m.name, c.name FROM sqlite_master m JOIN pragma_table_info(m.name) c WHERE m.type='table' AND (c.name = 'embedding' OR c.name LIKE '%_embedding')` against `.agentic-qe/memory.db` to discover new tables, then extend V2's list.
- **`agentdb@3.0.0-alpha.10` pin is a deliberate pre-pivot downgrade, NOT a forward-bump candidate** — Local Patch 17.1 pins agentdb to `3.0.0-alpha.10` in `.mcp.json` (rather than `@latest` or the previously-pinned `alpha.14`). Rationale: alpha.12 was an architectural pivot that **removed 7 controller families** — attention, reflexion, skills, causal, and learning-session (plus the core/pattern tool sets that hang off them). alpha.10 is the last pre-pivot release that still registers all of them as MCP tools (`agentdb_attention_*`, `reflexion_store/retrieve`, `skill_create/search`, `causal_add_edge/query`, `learning_start_session/train/predict/...`). The `mcp start` arg shape is **identical** across alpha.10↔alpha.14 (verified in alpha.10 CLI source at `dist/src/cli/agentdb-cli.js` `handleMcpCommand`), so the earlier alpha.13→14 arg-shape warning does not bite going backwards. agentdb writes only to `./agentdb.db` (additive `CREATE TABLE IF NOT EXISTS` schemas); it does not touch `.swarm/memory.db` (ruflo) or `.agentic-qe/*.rvf` (AQE), so the downgrade is data-safe (a `agentdb.db.alpha14-backup` snapshot is taken before first alpha.10 start as belt-and-suspenders). **Action — do NOT bump to alpha.12+ during quarterly review unless upstream has restored the 7 controllers OR an alternative path exposes them**; check the alpha.12+ changelog for controller re-introduction before considering any forward move. If staying on alpha.10, no action needed.

---

## Tier 10 — Objective reward oracle + Router-B production closure (this session)

The self-improvement work prior to this tier had a CLOSED-LOOP *mechanism* on the AQE router but two unresolved weaknesses: (a) the reward fed to the trainers was a constant (0.8) or a gameable prose-sentiment heuristic, and (b) Router B (`ruflo hooks route`) had no production training store the CLI could write, so its learning was inert on real turns. Tier 10 replaces the reward with an objective transcript-outcome oracle, and wires Router B's outcome store + a graded dist re-rank so the loop closes end-to-end in production. **Honest scope is documented inline in every helper** (and carried into the R&D handoff `docs/whats-genuinely-left-rnd.md`): the loop is now CLOSED and the mechanism is proven on both routers, but production *efficacy* is NEUTRAL — held-out accuracy is flat and the signal is sparse (most real turns succeed). See the R&D doc for what remains.

**(29) Objective outcome reward oracle (`assets/claude-helpers/_derive-outcome.cjs`, `DERIVE-OUTCOME-V1`).** ⚠️ **Superseded by Patch 36 (`DERIVE-OUTCOME-V2`)** — the flat `0.7`-base scheme below was near-constant on real turns (R&D gate 2) and is replaced by a graded two-regime reward; kept here for history. Replaces the prior prose-sentiment `deriveReward` (which keyword-counted the agent's own final text — gameable, and near-always-true on real turns) with a reward grounded in what ACTUALLY happened in the turn. **Scoring spec (`_derive-outcome.cjs:47-53`):** `reward = 0.7 − 0.25·errorToolResults − 0.15·failedBashExits − 0.25·testFailures + 0.10·(recovered?1:0)`, clamped to `[0.05,0.95]` (`clamp` rounds to 4dp at :57 so the 0.5 boundary is IEEE-754-stable), `success = reward >= 0.5`. `PEN_TEST_FAIL = 0.25` (:50) is deliberately tuned so a **lone** failing test dips a turn below the 0.5 success boundary. The key integrity property: `tool_result.is_error === true` is **harness-set, not agent-authored** — it cannot be talked away by an upbeat summary. **Turn-scoping (`lastTurnEvents`, :90-114):** scores the LAST turn that actually DID tool work, walking user-prompt boundaries from the end and skipping a tool-less closing-summary turn — the realism gate found last-turn-only scoping dropped session-wide errors (one transcript had 27 `is_error` in-session, 0 in its final summary turn). At Stop time the last turn IS the work turn, so this is a no-op there; it only matters for multi-turn input. **D13 / SCOPE-HONESTY caveat (carried from `_derive-outcome.cjs:34-43`):** this is an outcome *proxy*, not a proof of task correctness, and because real turns mostly succeed the derived boolean is positive on the majority of turns — the per-turn learning signal is **real but sparse**. Do NOT overstate this as "graded learning everywhere." **PURE + TESTABLE:** `deriveOutcome(eventsOrText)` does no I/O / no process exit; a `--selftest` mode (:203, :232) runs six crafted transcripts (clean / failing-tool / failing-test / recovered / mixed / no-tools). Installed FIRST in the `fix-aqe.sh` helper loop (:89) because its two consumers `require('./_derive-outcome.cjs')` relative to `__dirname`. Idempotent (cmp-skip), reversible (`.fixaqe-bak`), `node --check`-gated.

**(30) Stop-hook objective oracle + Router-B graded outcomes writer (`assets/claude-helpers/aqe-post-route.cjs`, supersedes the constant `post-route --success true` Stop hook).** Three sinks, all best-effort, never blocks Stop, always exits 0: **(1)** the AQE router via `aqe hooks post-route --success <real>` (the proven-movable path); **(2)** the ruflo side via `ruflo hooks post-task -s <real> -q <reward>`; **(3) WS2b** — writes one graded outcome per turn to `.claude-flow/routing-outcomes.json` (`recordRoutingOutcome`, :130) in the shape `{task, agent, success, quality, keywords, timestamp}` that the RUFLO-SEMRANK-V1 dist re-rank reads. **HONEST scope (from `aqe-post-route.cjs:11-22`, must not be overstated):** AQE `post-route` consumes ONLY the BOOLEAN — upstream quality is binary (true→0.675 / false→0.425) and the Q-update reward is `success?0.1:-1`, so the graded `[0.05,0.95]` value is DISCARDED by the AQE router; the graded value rides on `ruflo hooks post-task -q` and on the routing-outcomes store. **Poison guard:** the routed agent comes from a TRUSTED channel only — explicit `$RUFLO_ROUTED_AGENT`/`argv[3]`, else the fresh (<2h) `.ruflo-route.json` sentinel from Patch 31; the AQE `recommended` field is the WRONG vocabulary (`qe-test-architect`, not `tester`/`coder`) and is not turn-correlated, so it is never used. Unknown agent ⇒ SKIP the write — an empty store makes the re-rank an exact no-op (reversibility-by-data); a poisoned store would mis-rank, so empty is strictly safer. Wired by `fix-aqe.sh`: legacy constant group stripped (:147-148), wrapper wired (:149). Reversible (`.claude/settings.json.fixaqe-bak`). **D1 note:** the former inline comment "Router B store not CLI-writable today" on the `ruflo hooks post-task` call was corrected this session — it's now "the CLI post-task does NOT write routing-outcomes.json; THIS wrapper does (WS2b)."

**(31) Route capture for on-policy Router-B training (`assets/claude-helpers/ruflo-route-capture.cjs`, `RUFLO-ROUTE-CAPTURE-V1`, UserPromptSubmit).** Supplies the missing trusted routed-agent for Patch 30's WS2b store. On each user prompt it asks ruflo's OWN router (`ruflo hooks route --task <prompt>`) what it recommends and stashes `{task, agent, ts}` in `.claude-flow/.ruflo-route.json` (vocabulary-consistent with the re-rank's candidate agents). **Envelope-pollution guard (`isRoutableTask`, :39-44):** a `UserPromptSubmit` can fire for non-task meta-envelopes (`<task-notification>`, slash-command echoes, hook output, tool-result echoes) whose `prompt` would poison the store with non-task noise; the regex rejects them (companion guard mirrored in `aqe-post-route.cjs:86-91`, which also rejects a polluted sentinel). **ON-POLICY caveat (`ruflo-route-capture.cjs:17-22`):** this records the agent ruflo ALREADY picked, weighted by outcome — it reinforces/penalizes the existing policy but, because ruflo's router does NOT explore alternatives, it CANNOT discover a better route for a task it currently mis-routes. Closing the loop end-to-end is the point; measurable accuracy improvement still depends on signal density + (future) exploration (see R&D doc gate 1). Read-only except the sentinel; silent (stderr only); always exits 0. Wired by `fix-aqe.sh:157`. **Update: exploration has now landed — see Patch 37 (`RUFLO-ROUTE-EXPLORE-V2`); reward density was sharpened in Patch 36 (`DERIVE-OUTCOME-V2`).**

**(32) Graded semantic re-rank of Router B (`fix-ruflo.sh` `wire_semantic_rerank`, `RUFLO-SEMRANK-V1`, :565-626; called at both `force_nested_agentdb` sites :708, :732).** Injects a graded re-rank into the dist `…/@claude-flow/cli/dist/src/mcp-tools/hooks-tools.js` semantic path, immediately BEFORE the `// Get agents from semantic routing or fall back to keyword` anchor (where `semanticResult` is populated and the `>0.4` gate hasn't yet run). Blend: `score = score·(1 − W) + meanQuality·W` with `W = 0.25` (i.e. `score·0.75 + meanQuality·0.25`), where `meanQuality` is the mean graded `quality` recorded per candidate agent in `.claude-flow/routing-outcomes.json` (via the module-scope `loadRoutingOutcomes()`), then re-sorts. **EXACT no-op when the store is empty/absent** — the loops do nothing, so routing is unchanged (reversibility-by-data). Blend keeps scores in `[0,1]` so the downstream `>0.4` gate + `confidence = topMatch.score` semantics are preserved. Modeled line-for-line on `wire_real_spawn`. Idempotent (sentinel `RUFLO-SEMRANK-V1` short-circuits at :571/:609), reversible (`.semrank-bak`), `node --check`-gated (restores backup on parse failure, :623). Anchor-based — if the upstream anchor string moves the patcher emits `ANCHOR_NOT_FOUND` (:613) and aborts cleanly rather than corrupting the file.

**(33) `.gitignore` — Router B runtime store excluded (`.gitignore:16-17`).** `.claude-flow/routing-outcomes.json` and `.claude-flow/.ruflo-route.json` are session/runtime learning STATE, not source, so they are gitignored (the durable artifacts are the tracked helper sources + the `fix-*.sh` re-apply logic). **Commit guidance:** do NOT commit these two files; they regenerate at runtime from the wired hooks. **Extended by Patch 39** — the Router-B explore-store files `.claude-flow/.ruflo-explore-state.json` + `.ruflo-explore.jsonl` are now also gitignored (`:18-19`).

**(34) `selfimprove-bench.cjs` instrumentation — same-scorer trend + AQE-router arm (`tools/selfimprove-bench.cjs`).** The re-runnable, READ-ONLY measurement harness (appends `.claude-flow/selfimprove-history.jsonl`) gained integrity instrumentation this session: **(a)** a `scorerVersion = 'norm-v1'` tag (:196) — the trend is read ONLY across rows with the SAME scorer (`sameScorer` filter :202; ≥3 such runs required before a trend is even considered, :206-209), because a label-map/task-set/hit-rule change is a metric REDEFINITION, not learning (a prior audit caught a phantom +8.3pp that was purely a normalization change). **(b)** `accuracyRawPct` (:112) reported alongside the normalized `accuracyPct` (:111) for transparency — a normalization change must never read as learning. **(c)** `methodCounts` histogram (:89, :97) of which router path decided each route (semantic-native / semantic-pure-js / keyword / unknown). **(d)** an AQE-router diagnostic arm `measureAqeRouter()` (:119-150) reading `rl_q_values` (qMean/qMin/qMax/qSpread, distinct-reward count, optional `--aqe-confidence` mean) — this measures the OTHER router (the one the objective oracle now actually feeds) in parallel; it is labeled diagnostic and does NOT change the headline Router-B verdict. The `LABEL_EQUIV` alias map (:76-82) is kept deliberately TIGHT (an over-broad map manufactures accuracy) and the prior `memory-specialist` entry was REMOVED. **Significance:** because Patch 30 now feeds `rl_q_values` a derived (non-constant) reward, the harness's prior `reward CONSTANT ⇒ NOT-IMPROVING` blocker can clear — the verdict gate at `:215` keys on `state.rewardIsConstant`.

---

## Tier 11 — Dream-lock hardening + Router-B exploration + graded reward (this session)

A swarm audit of the LIVE learning subsystem this session found three things Tier 10 left open: (1) the AQE DreamScheduler was silently failing **~50% of consolidation cycles** under concurrency ("database is locked"), accumulating orphaned `status='running'` rows and a never-checkpointed WAL — a real plumbing **regression of the dream/consolidation path**, not a Tier-10 artifact; (2) the Tier-10 reward, though objective, was **near-constant** on real turns (the R&D doc's gate 2); (3) Router B still had **no exploration** (the R&D doc's gate 1, explicitly flagged in Patch 31). Tier 11 fixes the dream lock across all four insert paths, replaces the flat reward with a graded two-regime reward, and adds ε-greedy exploration. **Honest status (carried into `docs/whats-genuinely-left-rnd.md`): self-LEARNING is now operationally trustworthy; self-IMPROVEMENT is wired and measurable but NOT yet proven** — the longitudinal proof needs cross-session data (R&D gate 3).

**(35) Dream-engine multi-path SQLite-lock fix (`fix-aqe.sh` `AQE-DREAM-LOCKFIX-V2`).** Root cause (corrects the initial audit assumption): `busy_timeout` is ALREADY maxed at 60s (`dream-engine.js:243`; the in-code comment #461 at `hooks-dream-learning.js:54-55` states busy_timeout *cannot* help), so the dominant 86×"database is locked" is a **SIMULTANEOUS-WRITER race**, not sequential contention — concurrent hook subprocesses read a stale `dreamState.lastDreamTime`, all race past the trigger, run parallel ~10s cycles, and hit the WAL writer at once; the pre-existing "already-running" guard had a **TOCTOU race** (all peek before any inserts its `running` row). Three sub-fixes, codified in `fix-aqe.sh` (sentinel + `.dream-lockfix-bak`, modeled on `AQE-PROMOTE-V1`), `node --check`-gated, applied across **ALL FOUR** `dream_cycles` INSERT paths — **a V1 that covered only the hook path REGRESSED in production** (the daemon drives cycles through the *engine* path, so 1-of-4 coverage let locks recur 86→101). **(1a) ATOMIC CLAIM** — `INSERT … SELECT 'running' … WHERE NOT EXISTS (recent running within -5 min)` then a `changes()===1` gate; losers bail. Replaces the TOCTOU guard; exactly one writer wins by SQLite write-serialization. Paths: `hooks-dream-learning.js` (hook), `dream-engine.js:625` (engine/**daemon**), `mcp/bundle.js` (MCP), `cli/chunks/chunk-IJ4BUSJN.js` (CLI). **(1b) WAL-CHECKPOINT CALLER** — `wal_checkpoint(TRUNCATE)` after every terminal cycle exit (completion/cancel/catch); the method existed (`UnifiedMemoryManager.checkpoint`) but had **ZERO callers**, hence the 4.16 MB WAL. **(1c) ORPHAN SWEEP** — both a startup sweep (`initialize()`) AND a **per-cycle** sweep (start of `dream()`) so a long-lived daemon self-heals without a restart: `UPDATE … status='failed' … WHERE status='running' AND start_time < -10 min`. **DATETIME-FORMAT bug (caught in review):** `start_time` is stored as JS `toISOString()` (`…T…Z`), so the predicates MUST use `strftime('%Y-%m-%dT%H:%M:%fZ','now',…)`, **not** `datetime('now',…)` (space-format) — SQLite's TEXT lexical compare is degenerate for same-day rows (`'T'`(84) > `' '`(32)), making the `-5min` claim window always-true and the `-10min` sweep always-false within the UTC day. One-time cleanup of the existing orphans ran with a mandatory timestamped `memory.db` `.bak`. **Verified:** 8-way concurrent burst → exactly one winner, `running` stays ≤1, **0 new locks**; the sweep clears a seeded same-day orphan; `running` 24→0. **Watch-item:** witness a clean *completed* cycle + post-cycle WAL drain on the next live dream interval. Requires a daemon restart to go live — see Patch 40.

**(36) Graded two-regime reward (`assets/claude-helpers/_derive-outcome.cjs`, `DERIVE-OUTCOME-V1 → V2`).** **Supersedes Patch 29.** The Tier-10 oracle was objective but near-constant on real turns (R&D gate 2: `routing-outcomes.json` showed 49/49 success, quality ∈ {0.7, 0.55}). V2 makes the reward DISCRIMINATE via two regimes: **failure band** `0.45 − 0.20·errors − 0.15·bashExits − 0.20·testFails + 0.05·recovered − retryPen`, always clamped **< 0.5**; **success band** `0.70 + editAdj(+0.20 @≤1 edit → −0.15 @≥8 edits) − toolSpamPen(>12 calls) − retryPen`, floored 0.55; overall clamp `[0.05,0.95]`. New signals `editCount`/`toolCallCount`/`retries` + a negative-mining `failureTag` (tool/test/recovered). `deriveOutcome()` signature preserved (consumers `aqe-post-route.cjs`, `ruflo-train-subagent.cjs` unaffected). `--selftest` extended to **11 cases** incl. ordering `efficient > churny > recovered-failure > hard-failure`. **Verified:** 5+ distinct reward values, max-pin ~33% (was the degenerate {0.7, 0.55}). The Patch-30 caveat still holds — the AQE router consumes only the boolean, so the graded value rides on `ruflo hooks post-task -q` + the routing-outcomes store; honoring graded quality in the AQE Q-update remains a deferred bundled-dist patch. Installed via `fix-aqe.sh` (covers both the `assets/claude-helpers/` source and the `.claude/helpers/` copy).

**(37) ε-greedy exploration in Router B (`fix-ruflo.sh` `wire_route_exploration`, `RUFLO-ROUTE-EXPLORE-V2`).** **Closes R&D gate 1** (the on-policy "cannot discover a better route" caveat in Patch 31). Injects exploration at the deterministic pick in the dist `…/@claude-flow/cli/dist/src/mcp-tools/hooks-tools.js` (was: always `semanticResult[0]` after the `RUFLO-SEMRANK-V1` sort). Policy: **dedup** the eligible (`>0.4`) candidates by captured agent (`metadata.agents[0]`, keep highest-score per distinct agent; source scan widened to 8 to surface up to 3 DISTINCT agents), then ε-greedy **over distinct agents** — prob `(1−ε)` exploit `[0]`, prob `ε` (requires ≥2 distinct) uniform-sample a *different* agent; `<2` distinct ⇒ forced exploit. ε = 0.15 linear-decay → 0.05 over the first 200 routed tasks (counter in `.claude-flow/.ruflo-explore-state.json`); env `RUFLO_ROUTE_EPSILON` overrides (clamped `[0,1]`); **`RUFLO_ROUTE_EPSILON=0` is an EXACT no-op** (regression-safe + the Gate-3 control arm). Logs each decision `{decision, chosenAgent, exploitAgent, epsilon, distinctAgents, score, ts}` to `.claude-flow/.ruflo-explore.jsonl`. Exploration happens AT THE PICK so the *explored* agent (not `[0]`) is what `ruflo-route-capture.cjs` captures and the reward trains. **Bug caught in review (V1→V2):** V1 sampled by index over a top-K with DUPLICATE agent labels, so 8/11 "explore" decisions re-picked the same agent (agent-level no-op); V2's dedup-by-distinct-agent guarantees `chosenAgent != exploitAgent` on explore (verified **0 no-ops over 5000 trials** + a live ~33% explore-rate with genuinely distinct alternatives). Idempotent (sentinel bumped to V2 to avoid stale-skip; V1 fully removed via restore-from-`.explore-bak`-then-repatch), reversible, `node --check`-gated, `try/catch` fallback to `semanticResult[0]`. `RUFLO-SEMRANK-V1` coexists untouched.

**(38) Control-arm toggle + AQE-router confidence threshold.** **(a)** `aqe-post-route.cjs` gained `RUFLO_DISABLE_TRAINING=1`: it computes + logs the would-be reward (breadcrumb still fires) but performs **NO learning writes** (skips AQE post-route, `ruflo` post-task, and the routing-outcomes store) — combined with `RUFLO_ROUTE_EPSILON=0` this is the **no-train CONTROL arm** for the longitudinal proof (R&D gate 3). **(b)** `.agentic-qe/config.yaml` `routing.confidenceThreshold 0.7 → 0.6` (live confidence ~0.66 sat below the 0.7 bar, so the AQE ML route rarely fired). **Scope:** gates ONLY the AQE ML router; Router B uses its own `confidence = topMatch.score` with the hard `>0.4` gate and is unaffected. The `routing` block is user-owned + preserved across `aqe init`, and a 2-line intent comment was added above it. **(Update: now also CODIFIED in `fix-aqe.sh` Step 4 — see Patch 41 — so a stock regen that resets it to 0.7 is repaired; the earlier "deliberately not codified" stance was reversed on request.)**

**(39) Bench control arm + explore-store gitignore.** **(a)** The longitudinal protocol (R&D gate 3) runs `selfimprove-bench.cjs` ≥3× as a TRAINED arm (`RUFLO_ROUTE_EPSILON=0.15`, training on) and ≥3× as a CONTROL arm (`RUFLO_ROUTE_EPSILON=0` + `RUFLO_DISABLE_TRAINING=1`) under the SAME `scorerVersion=norm-v1`, into SEPARATE histories (`selfimprove-history.jsonl` vs `selfimprove-history.control.jsonl`) so the series never mix; **pass = trained-up ≥5pp AND control-flat, at ≥2σ**, reporting `accuracyRawPct` alongside the normalized headline. First same-scorer datapoints: **both arms flat at 16.7%, zero separation — EXPECTED in-session** (no live work flows between bench runs); proof requires cross-session runs. Do NOT relax the scorer or the gate. **(b)** Extends Patch 33: `.claude-flow/.ruflo-explore-state.json` + `.claude-flow/.ruflo-explore.jsonl` added to `.gitignore` (`:18-19`) — runtime learning STATE, regenerated at runtime; do NOT commit.

**(40) OPERATIONAL FACT — dist patches need a daemon restart to go live.** The ruflo/AQE daemon is a **long-lived node process that loads the global `dist` into memory at startup**; patching `dist` on disk (even via `fix-aqe.sh`/`fix-ruflo.sh`) does NOT affect the running daemon until it restarts. This caused the Patch-35 V1 "regression" illusion — the on-disk fix was correct but the daemon (pid 44886, up for a day) kept running the OLD engine and kept logging locks. There is **NO `ruflo daemon restart` subcommand** and no launchd supervisor: run `ruflo daemon stop` → (do DB orphan cleanup while quiescent — every `running` row is then definitionally orphaned and safe to sweep) → `ruflo daemon start`, and confirm the pid changed + status healthy. Corollary baked into Patch 35: prefer self-healing logic (the per-cycle sweep) over startup-only logic, since the daemon can stay up for days.

**(41) Codified config threshold + tracked `.claude/commands` docs in `fix-aqe.sh` (`AQE-ROUTING-THRESHOLD-V1`, `CLAUDE-CMD-DOCS-V1`).** Two edits from Tier 11 that lived in the untracked, regen-able trees (`.agentic-qe/`, `.claude/`) are now re-applied from the kit's tracked sources (`lib/` + `assets/`) so they survive a reinstall/regen. **(a) Step 4** value-idempotently sets `.agentic-qe/config.yaml routing.confidenceThreshold = 0.6` (kit constant `AQE_CONF_THRESHOLD` at the top of the step; `.fixaqe-bak` backup; a node scalar-replace `/^(\s*confidenceThreshold:\s*)\S+/m` that preserves the 2-line intent comment + the rest of the YAML; re-reads to verify the write took, restores on failure). Supersedes the Patch-38 "deliberately not codified" note. **(b) Step 5** installs kit-maintained docs under `.claude/commands/` from the NEW tracked source `assets/claude-commands/` — mirrors the `assets/claude-helpers/` → `.claude/helpers/` pattern (`find … -name '*.md'`, relative-path-preserving, `cmp`-skip, `.bak`, `--dry-run`); currently `analysis/COMMAND_COMPLIANCE_REPORT.md`. **Verified by a simulated regen:** resetting the threshold to 0.7 and deleting the installed report, then `bin/ruflo-kit fix-aqe <target>`, reported `2 change(s)` and repaired both (threshold → 0.6 with the comment intact; report reinstalled **byte-identical** to source); a re-run reported `0 change(s)`. Both idempotent + reversible; the `.claude/` + `.agentic-qe/` copies stay untracked — the tracked kit source (`lib/`, `assets/`, `tools/`) is the source of truth. The compliance report itself was also corrected this session (it had only catalogued 2 of the 5 analysis command docs and used `npx claude-flow` where the binary is the global `ruflo` — see the report's own "Key alignment finding").

---

## Safety notes

- **Dry-run mode**: `fix-ruflo.sh --dry-run` and `upgrade-2026-05-26.sh --dry-run` print every action they would take without modifying anything. Use this first if you're unsure.
- **Idempotency**: the three "fix-*" scripts can be re-run any number of times. They detect already-applied patches and skip them.
- **Logging**: `fix-ruflo.sh` and `upgrade-2026-05-26.sh` tee to `/tmp/fix-ruflo-*.log` and `/tmp/upgrade-ruflo-*.log` respectively. Keep these around for post-mortem if a step fails.
- **Scope**: the scripts touch user-scope locations only. No `sudo`, no system files, no production state. Recordings, project source code, and git state are untouched.
- **Network**: every step that needs the network catches errors and continues. If you're on an air-gapped machine, the `npm view`/`npm install` steps will fail loudly but the remaining diagnostic steps still run.
- **What is NOT modified**: the scripts never touch `.git`, never commit, never push, never re-write production source. They do not touch the project's `package.json` except to **remove** known-conflicting dev deps (and only after detection).

---

## FAQ

**Q. `agentdb_controllers` still says 15+ controllers disabled after I ran `fix-ruflo.sh` and restarted Claude Code.**
Run `bin/ruflo-kit fix-ruflo <target>` once more and look at Step 3/11 ("Shadow agentdb detection") and Step 10/11's "AgentDB import" output. A new shadow may have appeared (a fresh `npx` of an unrelated tool can re-introduce one). If the output looks clean and the issue persists, wipe the npx cache: `rm -rf ~/.npm/_npx && npx -y ruflo@latest --version && bin/ruflo-kit fix-ruflo <target>`.

**Q. `memory_store` works but `memory_search` returns nothing.**
That usually means the HNSW vector index is out of sync with the SQLite memory table. Confirm `.swarm/hnsw.index` exists and has a recent mtime; if not, store one entry to trigger rebuild: `npx ruflo@latest memory store --key bootstrap --value ok --namespace project`.

**Q. The status line says "▊ RuFlo + Agentic QE v3" with no live numbers.**
That's the literal fallback string in `statusLine.command`. Both Node scripts failed. Most often `.agentic-qe/memory.db` is missing — run `aqe init --auto` to create it, then `bin/ruflo-kit fix-statusbar <target>` to re-patch.

**Q. `ruflo-session-init.sh` reports "MCP server not running".**
Claude Code launches the MCP server lazily on the first MCP tool call. The warning is informational; once you do anything that needs MCP, the server will spawn.

**Q. Should I commit `.swarm/` and `.claude-flow/` to git?**
The persistent files (`memory.db`, `hnsw.index`, `graph-state.json`, `intelligence-snapshot.json`) are useful **per-developer** memory and can be committed if you want cross-session, cross-machine recall. The transient lock files (`*.lock`, runtime sockets) should be gitignored. See your project's `.gitignore` for the canonical list.

**Q. I'm on Linux / Windows / WSL — does this work?**
Linux is fully supported (the script branches on `uname` for the `sed -i` syntax difference). Windows is not supported directly; use WSL.

---

## Verifying everything works (one-page checklist)

```bash
# 1. First-time bootstrap (only needed once per project)
bin/ruflo-kit init /path/to/codebase
# Expected: "8/8 checks passed", "All systems ready"

# 1b. Per-session maintenance (every Claude Code session)
bin/ruflo-kit session /path/to/codebase

# 2. Expected: zero ERRORS in the final summary

# 3. Open Claude Code. Restart it if it was running.

# 4. In Claude Code, run these MCP tool calls (paste verbatim):
mcp__claude-flow__agentdb_health
mcp__claude-flow__memory_store --key healthcheck --value ok
mcp__claude-flow__memory_search --query healthcheck
mcp__claude-flow__hive-mind_status

# 5. Expected:
#    - agentdb_health: connected, version 3.x, controllers >= 15
#    - memory_store: returns entry_<timestamp>_*
#    - memory_search: returns the healthcheck entry
#    - hive-mind_status: clean (no ghost workers)

# 6. Confirm status line shows live counts (top-right corner of Claude Code)
#    Expect: "▊ RuFlo V3.10 …" + "🤖 Swarm ◉ [00/15]" + "▊ Agentic QE v3 🎓 N patterns"
```

If all six pass, the kit is doing its job.

---

## Credits & feedback

These scripts wrap workarounds for known issues in [`ruflo`](https://github.com/ruvnet/ruflo) and the [`@claude-flow/memory`](https://www.npmjs.com/package/@claude-flow/memory) packages as of Q2 2026. Several patches (the ESM `require` fix and the embedder argument) have been merged upstream; the scripts detect this and report "already present" without re-patching.

When reporting issues, include the full `/tmp/fix-ruflo-*.log` output and the result of:

```bash
node --version
npm list -g ruflo --depth=0
ls ~/.npm/_npx | head
find ~/.npm/_npx -path "*/agentdb/package.json" -exec node -e "console.log(require(process.argv[1]).version, '→', process.argv[1])" {} \;
```

---

*Licence: MIT. No warranty. Use at your own risk on a developer machine; never run blind on production infrastructure.*
