.env
*.db
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
dist/
build/
*.egg-info/
secrets/
data/
vendor/

# Router B runtime learning store (session state, not source)
.claude-flow/routing-outcomes.json
.claude-flow/.ruflo-route.json
.claude-flow/.ruflo-explore-state.json
.claude-flow/.ruflo-explore.jsonl

# ── Self-dogfood target state ────────────────────────────────────────────────
# This repo IS the kit; the canonical SOURCES live in assets/ (helpers, commands,
# builds) + lib/ + tools/. The trees below are runtime state generated when the
# kit operates on a target codebase (including this repo when dogfooding) — ignore
# them so a `bin/ruflo-kit` run never dirties the kit.
.claude/
.claude-flow/
.agentic-qe/
.swarm/
.hive-mind/
node_modules/
.mcp.json
.mcp.json.bak
# package.json + package-lock.json are TRACKED: they are the kit's own vitest
# manifest, which CI (nightly-drift "npm ci" + "npm test") depends on. They are
# kit source, not dogfood runtime state — do not re-ignore them.
agentdb.*
*.rvf
*.rvf.lock
*.rvf.idmap.json
.DS_Store
CLAUDE.md.bak*
CLAUDE.md.backup
CLAUDE copy.md

CLAUDE.md
# npm distribution research — local planning doc, not published with the kit
docs/NPM-DISTRIBUTION-RESEARCH-2026-07-18.md
# intelligence/memory audit — local snapshot report, not published with the kit
docs/INTELLIGENCE-EXEC-SUMMARY.jsx
docs/INTELLIGENCE-EXEC-SUMMARY.html
# memory-grounded project two-pager — local memory-proof snapshot, not kit source
docs/KIT-MEMORY-GROUNDED-2PAGER.md
# ── Local session artifacts: kept on disk, never published ───────────────────
# Background research, per-session audit ledgers and dated session changelogs.
# They are working notes for a specific session, not kit source or operator
# documentation — purged from history 2026-08-06 and ignored from here on.
# Anything referencing them must stand on its own without the link.
docs/reference/
docs/gauntlet*/
docs/CHANGELOG-2026-07-17.md
docs/STATUSLINE-AUDIT-2026-07-18.md
